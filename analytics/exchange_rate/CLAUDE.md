# analytics/exchange_rate/ — Panorama Cambial

Self-contained HTML report on Brazilian FX fundamentals (`reports/fx_report.html`) — single file, opens in any browser, safe to email. Reads from `macro_brasil` and `macro_international` (see [`domain/db/CLAUDE.md`](../../domain/db/CLAUDE.md) for schema-naming conventions). This is the applied/analytics branch of the exchange-rate work — see root `CLAUDE.md`'s `repository/` section for how it relates to the literature-curation and consolidated-synthesis branches.

## Generating the report

```powershell
uv run python jobs/update_db.py             # refreshes macro_brasil (cmb_reservas_bc, cmb_cambio_contratado, cmb_ptax, cmb_balanco_pagmt, cmb_fluxo_cambial, cmb_termos_troca, cmb_comex_*, atv_pib_usd)
uv run python jobs/update_international.py  # refreshes macro_international (cmb_reer, cmb_cot_fx, diferenciais_juros)
uv run python -c "from analytics.exchange_rate.generate_report import run; run()"
# → reports/fx_report.html (~50 KB)
```

Updating the DB first is optional — only needed for fresher data. `generate_report.py` alone re-renders against whatever is already in MySQL.

## Report architecture

Fixed template (`report.html`) with a `/*REPORT_DATA*/` marker inside a `<script>` block. `generate_report.py` loads each table, serializes to JSON, and does a plain `str.replace()` — no Jinja2, no templating engine. Every `_load_*()` function is independently try/excepted, so one missing or broken table degrades just that section (prints a warning) instead of failing the whole report.

Six tabs, real switching via JS `display` toggling (not scroll anchors): Balanço de Pagamentos (`tab-bop`, plus a companion BOP heatmap at `tab-heatmap`), Posicionamento do BCB (`tab-bcb`), Fluxo Cambial (`tab-flow`), Cotação (`tab-quotation`), Valuation (`tab-valuation`).

## Section → schema → table mapping

| Report tab | Loader (`generate_report.py`) | Schema | Table(s) |
|---|---|---|---|
| Cotação | `_load_ptax` | `macro_brasil` | `cmb_ptax` |
| Valuation | `_load_diferenciais` | `macro_international` | `diferenciais_juros` |
| Valuation | `_load_reer` | `macro_international` | `cmb_reer` |
| Valuation | `_load_cot_fx` | `macro_international` | `cmb_cot_fx` |
| Valuation | `_load_termos` | `macro_brasil` | `cmb_termos_troca` |
| Posicionamento do BCB | `_load_bcb_positioning` | `macro_brasil` | `cmb_reservas_bc` |
| Fluxo Cambial | `_load_fluxo` | `macro_brasil` | `cmb_fluxo_cambial` |
| BOP + Heatmap | `_load_bop` | `macro_brasil` | `cmb_balanco_pagmt` (+ `atv_pib_usd` for the "% of GDP" toggle) |
| BOP — by partner country | `_load_comex_pais` | `macro_brasil` | `cmb_comex_pais` |
| BOP — by aggregate factor | `_load_comex_fator_agregado` | `macro_brasil` | `cmb_comex_fator_agregado` |
| BOP — by product | `_load_comex_produto` | `macro_brasil` | `cmb_comex_produto` |

Note: interbank FX volume (`fx_interbank_vol_t1`/`t2`) lives in `cmb_ptax` but is charted under Fluxo Cambial, not Cotação — Cotação shows only the spot level.

`agent_data.py` (`get_fx_snapshot()`, consumed by the `cambio-analyst` subagent) reuses these same `_load_*()` functions and reduces each series to latest value + 1m/3m/12m deltas + a `stale` flag (per-group expected-gap thresholds hardcoded in `_EXPECTED_GAP_DAYS`).

## Current data-quality gotchas

- **`cmb_ptax.ptax_venda` starts 1994-07-01, not 1984** — SGS 1 technically goes back to 1984-11-28, but everything before 1994-07-01 is denominated in extinct pre-Real currencies (Cruzeiro Real and, further back, Cruzeiro/Cruzado/Cruzado Novo), confirmed directly in the data by the 1994-06-30 (2750.00) → 1994-07-01 (1.00) break at exactly the Real's fixed launch parity. Pre-Real rows were deleted from the DB 2026-07-22 (not economically comparable without a conversion factor this project doesn't carry); `cmb_ptax.py`'s `_START_YEAR`/chunking was updated so a future `run(start="all")` backfill won't reintroduce them.
- **Financial Account sign flip is deliberate** (`_load_bop()` only — never touches the DB): `idp_exterior`, `portfolio_ativos`, `outros_inv_ativos`, `acoes_ativos`, `fundos_ativos`, `titulos_ativos_cp`, `titulos_ativos_lp`, `derivativos`, `ativos_reserva`, and `conta_financeira` are negated so the whole report reads "negative = USD outflow, positive = inflow" consistently, matching how Current Account already reads. Liabilities-side series (`investimento_direto_liquido`, `portfolio_passivos`, etc.) already publish in that convention and are left untouched. If you touch this function, re-derive the sign from a real month rather than assuming Assets/Liabilities are symmetric.
- **Units are not uniform:** `cmb_balanco_pagmt`, `cmb_reservas_bc`, `cmb_fluxo_cambial` store USD MM (divided by 1000 for display). `cmb_comex_pais`/`cmb_comex_fator_agregado`/`cmb_comex_produto` store raw USD (divided by 1e9).
- **Comex Stat breakdowns ≠ BPM6:** the by-country/by-factor/by-product tables use general-trade methodology (SISCOMEX), not BPM6 — their totals will not reconcile line-for-line with `cmb_balanco_pagmt.mercadorias_gerais`. Treat them as complementary cuts, not a decomposition of it.
- **Some "missing" months in Comex/product series are real zeros, not gaps** (e.g. `demais_import` in the Fator Agregado breakdown, `minerio_ferro_import`/`petroleo_export` in the product breakdown — confirmed as months with zero transactions, not pipeline failures). `generate_report.py` `fillna(0)`s these deliberately; do the same for any new derived series built on top.
- **`lucros_reinvestidos` (BCB SGS 22815) has no data 1999–2010** (confirmed 404 from the BCB API, not a pipeline bug) — already `fillna(0)`'d before summing into `lucros_dividendos`.
- **No "gross reserves" series** — SGS 13127 (`reservas_brutas_usd`) times out consistently (wrong/discontinued code); resolved by using the liquidity concept (`cmb_reservas_bc.reserves_liquidity_daily`) plus its detailed components instead. Not a gap to revisit.

## Reference material (`referencia/`)

Not read by any script — background only:
- `balance_payments_breakdown.xlsx` — the official SGS-code mapping behind `cmb_balanco_pagmt.py`; check this before adding or changing BOP series.
- `fx_forecasting_theory_vs_practice.md` and `fx_forecasting_literature_review.md`/`.pdf` — standalone writeups on FX forecasting theory vs. practice (UIP failure, scapegoat theory, terms-of-trade channel, with a Brazil-specific section).
- `er_forecasting/` — the 9 underlying academic papers those two documents draw from.
- `state_space_equilibrium_model.md` — concept note (not yet implemented) for a Kalman-filter BEER-style model treating "equilibrium" USD/BRL as an unobserved state, with carry/terms-of-trade/fiscal-credibility as pull variables — the planned integration step once `carry_model.py`, `terms_of_trade_model.py`, and `fiscal_credibility_model.py` all exist standalone. Has open threads — see the "State-space equilibrium model" subsection under `models/` below.
- `state_space_simulator.html` — self-contained interactive dashboard (no build step, open directly in a browser) letting you hand-tune the concept note's 3 equations (driver shapes plus γ_ppp, φ, β_carry, β_tot, β_fiscal) and watch the simulated equilibrium/deviation/observed-rate paths. Synthetic data only, not fit to real BRL series. Same known sign-convention inconsistency flagged above. **Rebuilt 2026-07-24 across two same-day revisions** (user-initiated design discussion, then a direct challenge to the first revision's own equilibrium equation):
  - *First revision*: made all drivers enter contemporaneously (time t, not t−1) — financial variables adjust in real time, so a lagged spec mostly discards the real relationship and keeps noise, which is exactly why `carry_model.py`'s lagged tests came back so weak.
  - *Second revision, same day* (user caught two problems with the first): (i) the transition equation was adding a driver's raw *level* every period, which for a "permanent shift" (Step) shape produced unbounded compounding drift instead of a one-time shift — fixed by driving the deviation equation off each channel's period-over-period *change* (Δ) instead; (ii) the first revision had moved `carry` into the transition equation using an I(1)/I(0) argument borrowed out of context from `bayesian_deviation_model.py`, without checking that the real fitted model already treats **PPP as the entire equilibrium** and regresses the deviation from it on all four channels (carry, terms-of-trade, breakeven, fiscal) together — never splitting any of them into a separate transition-equation role. Fixed by making equilibrium an explicit relative-PPP path (`π_diff`, a synthetic inflation-differential proxy — since it's a *rate*, accumulating its raw level is the correct construction, unlike the stock-level drivers) and moving carry/terms-of-trade/fiscal *all* onto the deviation side as Δ-regressors, matching `bayesian_deviation_model.py` exactly. φ's default is now 0.98, matching the ~44-month mean-reversion half-life that model's error-correction follow-up found in real data. Does not model that same model's own unexplained drift/intercept (~0.2–0.4/month) — a known simplification.
  - Also gained click-to-isolate legend items on the main "Observed vs. equilibrium" chart (click a legend entry to hide/show that line; y-axis rescales to what's left).
  - *Third pass, same day*: default π_diff shape changed from Trend to a new Constant option — a steady (non-widening) inflation differential is the more realistic baseline and produces the expected straight-line equilibrium; the inherited Trend shape ramps π_diff's own level, which once accumulated produces an ever-accelerating (quadratic) equilibrium path instead — caught by the user directly from the chart's shape, not anticipated in advance.
- `ppp_dashboard.html` (generated 2026-07-23 by `models/ppp_equilibrium.py`, see below) — real-data companion to the concept note's equilibrium question: relative PPP (headline IPCA/CPI price-level ratio) as a candidate for the measurement equation's equilibrium value, with a client-side base-month selector (any month 1994-07→latest) so you can see directly how base-sensitive the PPP-implied level is. Uses the plain PTAX convention (rate up = BRL weaker) rather than the REER convention, so it doesn't inherit the sign-convention issue flagged above — a separate, unambiguous object from the simulator's `rer`. Rebuilt 2026-07-23 in the Lis Capital dashboard style (`.claude/skills/lis-dashboard`: Chart.js 4.4.1 + chartjs-plugin-datalabels via CDN, Barlow/Barlow Condensed/JetBrains Mono via Google Fonts) — unlike `state_space_simulator.html`, this one needs internet access to render (fonts + chart lib load from CDN, not vendored). Extended same day with 4 raw-series panels (carry, terms-of-trade, 10y bond-implied breakeven inflation, fiscal risk — see `models/ppp_equilibrium.py` below) for visual inspection ahead of the planned Bayesian deviation model; each panel shows its own full history capped at 1994-07 (not padded/back-filled before its actual start), sharing the dashboard's month axis so timing across panels lines up. **Now two tabs** (added 2026-07-23, same day as the model was fit): "Equilibrium & Data" (everything above) and "Bayesian Model" — the latter has the model explanation (equations, variable table, technique), descriptive stats + a per-spec diagnostics table (mean/sd/HDI/r-hat/ESS), a historical-fit chart (actual vs. fitted deviation with a 94% credible band), a cumulative decomposition in log/% terms (per-channel contribution to the modeled deviation change, stacked, sums exactly to actual), a second decomposition in **nominal BRL/USD terms** (added 2026-07-23, user request: "e=5, PPP suggests 4.8, carry adds 0.4, fiscal subtracts −0.5, residual completes the gap" — same log-space pieces, converted to a real-money bridge from the PPP-equilibrium level up to the actual rate; necessarily sequential/order-dependent since going from log-additive to level-additive is a multiplicative-to-additive conversion, not a second independent split — order fixed as equilibrium → baseline (α + pre-sample anchor) → carry → tot → breakeven → fiscal → residual, verified to reconstruct the actual rate exactly to ~1e-4), a cross-spec forest plot, and posterior-distribution histograms for the primary spec's parameters. Tab-switching is lazy (Bayesian tab's Chart.js instances only construct on first click, avoiding the zero-size-canvas-in-a-hidden-div problem). **Generating the full dashboard (both tabs) now requires `bayesian_deviation_model.render_dashboard()`, not `ppp_equilibrium.run()` alone** — see that module's entry below; `ppp_equilibrium.run()` still works standalone but leaves the Bayesian tab showing a "no data" placeholder (the template's `/*BAYES_DATA*/` marker defaults to `null` when not supplied). **Chart fix, 2026-07-23**: the log/% decomposition chart and the nominal R$ bridge chart were originally built as Chart.js `type:'line'` with `fill:true` + `stacked:true` scales — Chart.js does **not** actually stack line fills that way (each area independently fills to zero and overlapping translucent layers just blur together, confirmed "horrible" by direct user feedback with a screenshot). Fixed by converting both to real `type:'bar'` with stacked scales (the R$ bridge keeps "Actual (PTAX)" as a `type:'line'` overlay dataset in the same mixed chart — Chart.js supports per-dataset `type` override). First attempt at the fix also set `barPercentage:1.0, categoryPercentage:1.0` (no gaps), which still reads as a continuous area rather than "bars" — corrected to `0.85`/`0.9` after the user supplied a reference screenshot of what a proper gapped stacked-bar chart looks like. Lesson: a stacked-bar fix isn't complete without visible gaps between bars.

## `models/` — research track (not wired into any report)

Statistical models testing FX theory directly against this database — distinct from `generate_report.py`, which only displays raw series, no estimation. Goal (stated 2026-07-22): understand where simple models fail at explaining USD/BRL. Originally scoped around three candidate channels — carry, terms-of-trade, fiscal credibility — but terms-of-trade and fiscal credibility were dropped 2026-07-23 (see below) in favor of going straight to PPP as the state-space equilibrium anchor. See `models/DATA_REQUIREMENTS.md` for the (now partly superseded) data-availability mapping across all three.

`ppp_dashboard.html`'s charts all have TradingView-style zoom/pan (drag to pan, scroll/pinch to zoom, Y auto-fits to what's visible, double-click to reset — updated 2026-07 to match TradingView's literal gestures, see below) — see [`.claude/rules/lis-dashboards.md`](../../.claude/rules/lis-dashboards.md) for the standing convention this establishes for LIS dashboards generally, not just this one.

- **`uip_model.py`** (`uip_model.md`) — the strict UIP test (β=1) on the short-rate nominal differential alone. OLS + GARCH(1,1) + 2-regime Markov-switching. Result: consistently negative β (wrong sign vs. UIP), matching the Fama-puzzle/Araújo et al. literature — though the regime assignment doesn't replicate their exact low-vol/high-vol split.
- **`carry_model.py`** (built 2026-07-22 — **`carry_model.md` not written yet**; do that, following `uip_model.md`'s pattern, before calling this model "done"). Broader, less restrictive than `uip_model.py`: 4 specs crossing horizon (short-rate Selic−Fed Funds vs. BR−US 2y point) × construction (nominal vs. real ex-post), plus a "dynamic" spec testing the *change* in the short-rate differential (`diff_a_nom_chg_lag1`) alongside a new `gl_mc` "global monetary cycle" variable (US 2y − Fed Funds, the US front-end curve slope). Sample windows differ by spec and were set from verified data, not assumption: short-rate specs run 1999-03→today; the BR 2y leg (`base_mercado.interest_rates`, curve `PREJS`, tenor `24M`) only starts 2006-01 — confirmed against Tesouro Direto's actual historical bond file that no genuine 2-year point existed before then (checked live, not assumed). All 5 specs: weak/insignificant, R² ≈ 0.002–0.004 — carry is not, on its own, a workable predictor in this data, consistent with `uip_model.py`'s own finding.
- **`ppp_equilibrium.py`** (built 2026-07-23) — not a regression, a data/dashboard generator: builds the relative-PPP equilibrium candidate (headline IPCA index ÷ headline CPI index, anchored to actual PTAX at a selectable base month) directly named in the state-space concept note's open thread below, and renders it to `referencia/ppp_dashboard.html`. Sample 1994-07→today (383 months), the full range where PTAX/IPCA/CPI all overlap. Rerun via `uv run python -c "from analytics.exchange_rate.models.ppp_equilibrium import run; run()"` — refetches BR IPCA + PTAX from MySQL and US CPI live from FRED (not cached in DB) each time. Same day, also extended to fetch four candidate channels for explaining the PPP deviation (now regressed — see `bayesian_deviation_model.py` below): `carry` = `diferenciais_juros.diferencial_nominal` (Selic − Fed Funds, from 1999-03 — Selic-target regime start, not a fetch gap), `tot` = `cmb_termos_troca.termos_de_troca_funcex` (Funcex PX/PM via IPEADATA, real history from 1978 trimmed to 1994-07+), `breakeven` = 10y bond-implied inflation expectation, `PREJS − NTNBJS` @ 120M tenor from `base_mercado.interest_rates` (**external** fund-ops schema, not this project's ETL — read live via `MySQLDataRequester`, same pattern `carry_model.py` already uses for its BR-2y carry spec), from 2006-01, and `fiscal` = `cmb_risco_pais.cds_5y_usd` (Brazil 5Y CDS USD, manually ingested from investing.com exports, from 2007-12). All four are left-joined onto the core monthly index (null before each one's own start — no back-fill), and charted as their own dashboard panels. `breakeven` added 2026-07-23 specifically as a long-term-inflation-expectation proxy, after confirming BCB Focus's own longest horizon (`expc_focus`, IPCA 24m) only reaches back to 2021-03 — too short to be useful. Also carries forward a real, documented fix for the confirmed `PREJS@120M` data bug (two bad windows, `2010-01-22`–`2010-02-05` and `2010-03-02`–`2010-03-04`, values ~3% vs. a true ~13%, isolated to that one curve/tenor — verified 60M and `NTNBJS`@120M clean over the same period) via `_PREJS_120M_BUG_WINDOWS` + linear time-interpolation in `_load_breakeven()` — this is the "fix it in production" ask from `state_space_equilibrium_model.md`'s inline note, finally done as reusable code rather than the prior throwaway plotting script (though the fix still lives in this project's code, not in the external table itself). Also gained `compute_deviation(df, base_month)` (the same base-month-invariant deviation math the dashboard's JS computes client-side, now available server-side for `bayesian_deviation_model.py` to reuse) and a `target`/`breakeven_gap` column pair (`macro_brasil.inflc_meta`, BCB SGS 13521, the CMN inflation target — annual, forward-filled to monthly; `breakeven_gap = breakeven - target`, the "de-anchoring gap" used as an alternative regressor construction in the Bayesian model). **Tab 1 restructured 2026-07-28** (user request): the 7 raw-candidate-channel panels (carry/tot/breakeven/fiscal/dxy/trade_pct_gdp/ca_pct_gdp), previously one static chart-container each, are now a single chart with a `<select>` dropdown (`CHANNEL_DEFS` in the template) — switching the dropdown destroys and rebuilds the Chart.js instance so the axis fully re-derives from the newly selected series (no shared/awkward scale, no manual autofit needed). `breakeven_gap` — until then computed in `load_data()` but never actually added to `build_payload()`'s returned dict — is now included and selectable, labeled "Breakeven De-Anchoring Gap." Applied the `lis-dashboard` skill's just-updated zoom/pan convention (`mode:'xy'`, free two-axis pan/zoom, no Y-autofit) rather than the older TradingView-style X-only+Y-autofit convention in `.claude/rules/lis-dashboards.md` — turned out the dashboard's global zoom config had already been migrated to `mode:'xy'` in a prior session (see the inline JS comment dated "updated 2026-07 twice in one day"), so no charts needed touching for this; only the new dropdown-driven chart itself was new code. **`relative_carry` channel added 2026-07-28** (user hypothesis: what matters for the carry trade is Brazil's rate advantage relative to its LatAm peers, not just the bilateral BR-US differential): `_load_relative_carry()` computes Selic minus the equal-weighted average policy rate of MX/CL/CO/PE (`macro_international.cmb_policy_rates`, BIS WS_CBPOL) — AR deliberately excluded (BIS stopped updating it 2025-07, and its rate regime is a structural outlier vs. the other four inflation-targeters); equal-weighting chosen over any GDP/trade-weighted scheme since no such weighting data is wired into this project for this purpose. None of the four peers' own start dates (earliest MX 1998-11) shrink the model's binding sample (fiscal/CDS, 2007-12→), so this is a pure addition, not a new constraint. Added as its own dropdown entry in `CHANNEL_DEFS` (color `#6E8B3D`, an olive-green outside the 7-color official LIS palette — same "extra distinguishing color" practice already used for `breakeven_gap`/`trade_pct_gdp`/`ca_pct_gdp`, all 7 official colors being already spoken for by other channels/series on this tab). **`carry_vol`/`relative_carry_vol` + a z-score toggle added 2026-07-28, same session** (direct user request, three asks in one turn): (1) a "Z-score" toggle button next to the existing dropdown (`zscoreToggleChannel` in the template) transforms whichever channel is currently selected into z-scores — `(v - mean)/stdev` computed client-side over that channel's OWN non-null history (not the shared 383-month index, which would understate mean/stdev for late-starting channels like fiscal/breakeven) — swapping the chart's format/unit label to `σ` and leaving the underlying `D[key]` data untouched; (2) `carry_vol` = `carry` ÷ BRL's own trailing-6-month annualized realized volatility (log returns of daily PTAX, `sqrt(252)`-annualized, standard construction) — a carry-to-volatility ("Sharpe-style") measure of Brazil's rate advantage per unit of FX risk, rather than the raw differential alone; (3) `relative_carry_vol` = `carry_vol(BR) − mean(carry_vol(peer))` for MX/CL/CO/PE, each peer's own carry_vol built identically (peer policy rate − Fed Funds, over that peer's own trailing-6m vol vs USD) — same equal-weight peer-average convention as `relative_carry`, just applied to the risk-adjusted metric. Required new peer FX data that didn't exist in the DB yet: **`macro_international.cmb_fx_latam`** (new table, `PRIMARY KEY (date, country_code)`, mirroring `cmb_policy_rates`' own schema shape), populated by the new `domain/db/international/yfinance/cmb_fx_latam.py` (Yahoo Finance `MXN=X`/`CLP=X`/`COP=X`/`PEN=X`, daily, local currency per USD — same quoting convention as PTAX, AR excluded for the same reason `cmb_policy_rates.py` excludes it) — user explicitly chose proper DB ingestion (mirroring how DXY itself was ingested) over a live-fetch-only shortcut, given this data is now reusable by future models. Verified coverage (2003-01→2003-12 across the four peers, PE earliest at 2001-05) doesn't shrink the model's binding sample (fiscal/CDS, 2007-12→). Not yet wired into `jobs/update_international.py` — same "runs manually for now" status as every other recently-added international series (see root `CLAUDE.md`'s pending-items list, which should be updated to include it). Both new channels added as their own `CHANNEL_DEFS` dropdown entries only (colors `#9B5DE5`/`#F15BB5`) — **not** added to the Bayesian regression spec (`primary_contemp`) this round, since the user's ask was scoped to tab 1 visualization only.

**Z-score reference window fixed to 2000-01+, same day, follow-up round**: user spotted (via a screenshot of `carry`'s z-score chart reading "+0,00 σ", i.e. "right at the historical average") that the toggle's full-own-history mean/stdev was letting the 1999-03→1999-12 Real-Plan/Selic-targeting transition (rates spiking ~37 down to ~12 in under a year — a one-off adjustment episode, not a representative "normal regime" observation) drag the baseline up to almost exactly where today's level sits, making the "at the mean" reading a coincidence of that early spike rather than a genuine "carry is unremarkable right now" finding. Confirmed empirically (not assumed) that no pre-1999 data was involved (`carry` has zero non-null rows before 1999-03) before making any change — the leakage hypothesis itself was false, but the underlying concern about the early-spike period was valid. Fix: `zScoreArr()` now computes mean/stdev only from `D.months >= '2000-01'` (`ZSCORE_REFERENCE_START`), applied per-channel (channels that already start later than 2000-01 — fiscal, breakeven, breakeven_gap — are unaffected, since the cutoff only narrows a window that already started later). The full series is still **plotted** across its whole history — pre-2000 points (where they exist) are transformed using the same post-2000 mean/stdev and shown, not hidden, so the 1999 spike now correctly reads as a large outlier (~+5.9σ) instead of silently inflating the baseline it's compared against. `carry`'s own reference stats moved from (mean=10.62, sd=4.87, n=328, full-history) to (mean=10.38, sd=4.56, n=318, 2000-01+) — latest (2026-06) z moved from +0.0002σ to +0.053σ, both still near zero but the second one for the right reason.

**Same-day follow-up: the chart's own visible range capped to 2000-01 too**, not just the z-score calculation — direct user request ("cap the data graph to the same initial date jan/2000"). `renderChannel()` now slices both `D.months` and the selected channel's array to `CHANNEL_VIEW_START` (`'2000-01'`, the renamed/generalized former `ZSCORE_REFERENCE_START`, now serving both purposes) **before** anything else runs — `zScoreArr()` itself no longer does its own slicing, it just z-scores whatever `values` it's handed, since the caller now always hands it an already-2000+-restricted array. This means the 1999-03→1999-12 spike no longer appears on the chart in EITHER raw or z-score view (previously it was still plotted, just correctly flagged as an outlier in z-score mode) — confirmed the chart's own max in-view z-score dropped from +5.87σ (the excluded 1999 spike) to +3.26σ (the 2002 FX-crisis-era carry peak, now the genuine max within the displayed window). Channels starting after 2000-01 anyway (fiscal, breakeven, breakeven_gap) are unaffected, same as before.

**The Bayesian model itself changed too, same follow-up** — direct user instruction: "I want the model to use that information, not to calculate the mean and stv on the model sample." Until now, `fit_contemp_spec()` standardized every regressor using `_standardize(sample, ...)`, i.e. mean/std computed from `sample` = the model's own narrow fitting-sample overlap (2008-01+, forced by fiscal/CDS's late start) — meaning `carry`/`relative_carry`/`dxy`, which all have real history well before 2008, were having that longer history thrown away for a reason (fiscal's start) that has nothing to do with their own data availability. Added **`_standardize_ext(sample, reference, cols)`**: computes each column's mean/std from a separate `reference` frame instead of from `sample` itself, each column independently dropna()'d (columns don't need to share a common non-null window). `fit_regression()` gained an optional `reference` parameter; `fit_contemp_spec()` now builds `reference = deltas[deltas.index >= "2000-01-01"]` (`_REFERENCE_START`, matching the tab-1 toggle's own cutoff) and passes it through. `build_dashboard_payload()`'s own standardization call (used for the historical-fit reconstruction and decomposition) was updated to match **exactly** — using a different reference window there than what was actually fit would silently desync the decomposition from the real betas. Refit (r-hat=1.00 throughout, 38s wall-clock, n=222 unchanged — the *fitting* sample is still bound by fiscal's 2007-12 start regardless of the reference window, only the *standardization scale* changed): **carry −0.960 (HDI [−1.812,−0.069], still reliably negative, wider now), relative_carry +0.532 (HDI [−0.419,1.543], still crosses zero, no signal), fiscal +2.677 (HDI [2.185,3.139], essentially unchanged), breakeven_gap +0.115 (HDI crosses zero, unchanged), dxy +1.080 (HDI [0.649,1.531], essentially unchanged)**. fiscal/breakeven_gap barely moved (their own real start already postdates 2000-01, so the reference window is effectively unchanged for them); carry/relative_carry/dxy's betas rescaled since their standardization now reflects a longer, more representative history of "what a typical move looks like" rather than only the narrow 2008+ window.
- **`bayesian_deviation_model.py`** (built and fit 2026-07-23) — Bayesian regression of the PPP deviation on carry/terms-of-trade/breakeven/fiscal, "attempt one" ahead of the Kalman-filter state-space model ("attempt two", concept-stage below). Full design + results in `models/bayesian_deviation_model.md`. Pre-registered an ADF/KPSS stationarity check before writing any model code — result: `deviation`, `carry`, `tot` are I(1), `breakeven`/`fiscal` are I(0), so the model uses first differences uniformly (a deliberate "attempt one" simplification, not a claim that's optimal). Fit via PyMC/NUTS (4 chains, r-hat=1.00 throughout): **terms-of-trade is the one consistently-signed result across both the short primary sample (2008-02→today, P(β<0)=0.82) and a long carry+tot-only robustness sample (1999-05→today, P(β<0)=0.83)**; fiscal risk (CDS) has the strongest single-spec sign probability (0.94) but no longer-sample check exists for it; carry shows no reliable signal and its sign isn't even stable across samples, extending `uip_model.py`/`carry_model.py`'s own finding; **breakeven (and the breakeven-minus-target de-anchoring gap, which turned out empirically ≈identical once differenced) came back with the opposite sign from what was expected** — a genuinely open, unresolved finding (candidate explanations: a hawkish-response/credibility-premium channel, or simultaneity the 1-month lag doesn't fully clear), not something to wave away. Normal vs. Student-t error comparison favored Normal (no evidence of fat tails at this sample size). Added `pymc`+`arviz` as project dependencies (`uv add pymc arviz`) and, mid-session, a system-level MSYS2/MinGW-w64 g++ compiler (see `AMBIENTE.md`) after the no-compiler PyTensor fallback proved too slow — cut each model fit from 10+ minutes to ~35-40 seconds. `run()` saves each fit's posterior trace to `bayesian_results/*.nc` (gitignored — regeneratable, ~30-40 MB apiece) plus a summary CSV; `run()` must be called at least once before anything below can read them back. **`build_dashboard_payload()`** (added same day) loads those saved traces (`load_saved()`, `az.from_netcdf` — no refit) and derives everything the dashboard's "Bayesian Model" tab needs: per-spec diagnostics tables, a historical-fit reconstruction (posterior mean + 94% credible band, in both Δdeviation and cumulative-level units, anchored to the actual deviation level one month before the primary spec's sample starts), a point-estimate decomposition (each channel's cumulative contribution via its posterior-mean β, plus a residual term — sums exactly back to the actual cumulative change, verified to ~1e-4), posterior histograms, and a cross-spec forest-plot structure. **`render_dashboard()`** is now the canonical way to generate the full dashboard (both tabs) — it calls `ppp_equilibrium.load_data()`/`build_payload()` for tab 1 and `build_dashboard_payload()` for tab 2, then a single `ppp_equilibrium.render()` call fills both template markers at once. **Tab 2 fully rebuilt 2026-07-28** (direct user request, "full replacement" over "add alongside" per an explicit choice): the whole 4-spec comparison (`primary_breakeven`/`primary_gap`/`primary_studentt`/`robustness`) was replaced by a single new **not-lagged** spec, `primary_contemp` — carry, fiscal (CDS), the breakeven de-anchoring gap, and DXY, entered as `Δchannel(t)` (no extra 1-month lag) via the new `build_deltas_contemporaneous()`, matching `state_space_model.py`'s own contemporaneous timing rather than this module's original lagged convention. Fit via the new `fit_contemp_spec()` (label `primary_contemp`; r-hat=1.00, 28s wall-clock): **carry −0.464 (HDI [−0.847,−0.074], reliably negative, the familiar "wrong sign vs UIP" reading), fiscal +2.651 (HDI [2.165,3.111], strongly positive as expected), breakeven_gap +0.133 (HDI crosses zero, no signal), dxy +1.030 (HDI [0.594,1.449], positive as expected, matching `state_space_model.py`/`beer_model.py`'s own DXY finding)** — n=221, 2008-01→2026-06. The old 4-spec battery's saved traces are untouched on disk (`bayesian_results/primary_breakeven_idata.nc` etc.) but `build_dashboard_payload()` no longer reads them — per direct user instruction ("it's not necessary to run the others especifications"), `run()` (the old battery) doesn't need to be re-run for the dashboard to work, only `fit_contemp_spec()` does. The cross-spec forest plot and the Normal-vs-Student-t LOO comparison were both removed along with the old specs (neither construct applies to a single spec on a new channel set), and the posterior-distribution histograms were dropped per explicit user request ("remove the posterior distribution of the bottom"). `models/bayesian_deviation_model.md` still documents the original lagged spec's design and results as-is — not rewritten, since that version's findings (e.g. terms-of-trade's cross-sample sign consistency) remain valid history, just no longer what the dashboard displays. **`relative_carry` added to `primary_contemp` 2026-07-28** (user hypothesis: regional rate positioning vs. LatAm peers, not just the bilateral BR-US differential, drives the carry trade) — alongside `carry`, not replacing it, per direct user choice (contrast with the "full replacement" call made earlier the same day for the 4-spec→1-spec battery). Refit (r-hat=1.00, 39s wall-clock, n=221 unchanged — `relative_carry`'s own coverage starts 1995-04, MX/CL/CO/PE all well before fiscal/CDS's 2007-12 binding constraint): **`relative_carry` itself shows no reliable signal (β=+0.405, HDI [−0.311, 1.115], crosses zero)**, but its inclusion visibly sharpens `carry`'s own estimate — β moved from −0.464 (HDI [−0.847,−0.074]) to −0.808 (HDI [−1.533,−0.085]), i.e. more negative and with a wider-but-still-zero-excluding HDI, once the regional-peer component is partialled out. Read honestly: this doesn't confirm the "relative to LatAm peers" hypothesis on its own terms (that channel's HDI still crosses zero), but it does suggest the bilateral BR-US carry signal was partly diluted by co-movement with the peer group in the original single-carry spec — a real, if modest, finding, not a null result to discard. Fiscal/breakeven_gap/dxy estimates essentially unchanged from the 4-regressor spec. `_SPEC_FILES["primary_contemp"]` and the level-bridge in `build_dashboard_payload()` were generalized to loop over `regressor_cols` (previously 4 hardcoded `lvl_N` variables) so the bridge works for any regressor count going forward, mirroring the same generalization `state_space_model.py`'s own level bridge already went through.

**Follow-up, same day: is the drift (α) real, or an artifact?** The primary spec's α (0.219/month) compounds into most of the current PPP gap — in the level bridge, "baseline" is +R$1.54 of the ~R$1.58 gap between equilibrium (R$3.48) and actual PTAX (R$5.06), dwarfing all four named channels (currently netting to ≈R$0.00). User pushback: "the PPP should be the only directional systematic drift along the time." Two tests, full detail + numbers in `models/bayesian_deviation_model.md`'s "Follow-up: testing the drift directly" section:
  - **No-intercept spec** (`primary_no_alpha`, α forced to 0): the 4 betas came back statistically unchanged — the drift didn't disappear, it just relocated from "baseline" to "residual." Why: standardized (z-scored) regressors have sample mean exactly 0 by construction, so none of them can ever explain delta_dev's actual non-zero mean (+0.222/month) — that mean can only land on the intercept, or, with none available, the residual.
  - **Error-correction spec** (`primary_ecm`, "Option B", user's own suggestion): adds `deviation_lag1` (the lagged deviation *level*) alongside the 4 channel deltas, α kept — but critically must be passed via the new `raw_cols=["deviation_lag1"]` argument to `fit_regression()` so it stays in native (non-zero-mean) units; a first attempt wrongly standardized it too, repeating the exact same mistake, caught and fixed before reporting results. Result: ρ (β_deviation_lag1) = −0.014, P(ρ<0)=89.2% (suggestive, not "reliable" by this model's own 94%-HDI bar) — implying a median half-life (among negative draws) of **~44 months**, matching Rogoff's classic "PPP puzzle" range (3-5 years) almost exactly. α *increased* (0.219→0.424) rather than shrinking — not a bug: α now means "drift at deviation=0," and since the sample's average deviation was well above zero with ρ weakly negative, some of the old "average drift" was actually reversion-from-an-elevated-start (a drag), so removing it pushes the zero-point intercept up.
  - **Net honest read**: weak-but-real evidence of PPP-consistent mean-reversion, at a textbook-matching magnitude — but even accounting for it, a sizeable unexplained constant pull toward BRL weakness survives. Neither test is wired into the dashboard or the Excel workbook yet (discussion-only round); the ~44-month half-life is flagged as a candidate concrete input for calibrating the state-space model's equilibrium-pull speed, next session.
- **`models/generate_model_spec_pdf.py`** (built 2026-07-28) — generates `reports/ppp_model_specs.pdf`, a written companion to `ppp_dashboard.html` meant to be sent alongside it: one section per tab (Equilibrium & Data, Bayesian, State-Space, Kalman, BEER, Rolling Window) with the equation, fitting methodology, sample window, numeric results (posterior means/94% HDIs), an explicit "Ressalva" (caveat) callout per section, a text-only pointer to the relevant dashboard chart (no embedded images, per user choice), and a closing cross-model synthesis — content in Portuguese (explicit user choice for this document, overriding the project's usual English-by-default rule for newly authored material), audience explicitly internal/technical (candor about "wrong sign vs UIP" findings, near-unit-root fragility, BEER's spurious-regression risk, etc. kept in, not softened). Pure `reportlab` (Platypus), no HTML/browser step — added as a new project dependency (`uv add reportlab`) since nothing in the repo previously wrote PDFs (`pdfplumber`/`pymupdf`/`pypdf` are all read-only). Uses DejaVu Sans (regular/bold/oblique/bold-oblique + mono), loaded straight from `matplotlib`'s already-installed `mpl-data/fonts/ttf/` rather than adding a font dependency — needed for full Greek/math Unicode coverage (α, β, φ, η, σ, π, Δ, Σ) that the PDF base-14 fonts (Helvetica/Courier) don't have. Content is hand-transcribed from this file's own model entries (not re-derived from a fresh model run) — if any model above is refit/reespecified, update this script's numbers manually in the same session, it has no live link to the underlying `.py` modules or the MySQL data. Two real rendering bugs caught and fixed before shipping, both by actually rendering and inspecting the output rather than trusting the code: (1) ReportLab's bordered/background `ParagraphStyle` (`borderWidth`/`backColor`/`borderPadding`) under-reports its own frame height for wrapped multi-line text, causing the box to visually overlap the flowable drawn just before it (here, each section's own "Especificação" heading) — fixed by rendering equations as a single-cell `Table` with cell padding/background instead of a bordered `Paragraph`, which reportlab handles correctly. (2) inline `<b>`/`<i>` markup inside `Paragraph` text silently fails to resolve to a custom TTF's bold/oblique variant unless `pdfmetrics.registerFontFamily()` is called explicitly — without it, the tag's color changes (if paired with an inline `<font color>`) but the weight/slant does not, which is easy to miss at a glance; caught only by inspecting actual per-character font names via `pdfplumber` (`char['fontname']`), not by eyeballing a screenshot. Verified via `pdfplumber`: full text extraction (checks Unicode symbols didn't become tofu/boxes) and per-page-to-image rendering (checks table/box layout across all 8 pages), not just `node --check`-style syntax validation. **Equations upgraded to real typesetting, same day** (direct user feedback that the monospace `β_carry·z(Δcarry(t))`-style text block "is not good enough" — read like source code, not math): each `equation_box()` now rasterizes proper mathtext (matplotlib, `mathtext.fontset="dejavusans"` to match the doc's own font) — real fraction bars, italic variables, upright `\mathrm{}` word-subscripts (`β_carry` → β with an upright "carry" subscript), Σ/∑ with under-set limits — into a transparent PNG via `fig.savefig(..., bbox_inches="tight")`, sized in Python (`PIL.Image.open().size` at the known 300 dpi) to fit the existing shaded/bordered box, replacing the old monospace `Paragraph`. The explanatory prose that used to be crammed inside the same box (via `<br/>`) was pulled out into a normal body paragraph below it — cleaner separation between "the equation" and "what it means." One `mathtext` incompatibility hit immediately: `\big(`/`\big)` (manual delimiter sizing, LaTeX-only) isn't supported and threw a `ParseFatalException` — replaced with `\left(`/`\right)` (auto-sizing, which mathtext does support) in the BEER-model sum. Page count grew 8→9 as a side effect (equation images take a bit more vertical room than the old monospace block did) — left as-is, not worth trimming spacers over.
- **`terms_of_trade_model.py` / `fiscal_credibility_model.py` — deliberately abandoned** (2026-07-23, explicit user call, time-constrained): the state-space work now proceeds directly from PPP as the equilibrium anchor instead of waiting on these two as prerequisites. Don't restart them without re-confirming scope. (Note: this is distinct from the `tot`/`fiscal` *data series* now fetched in `ppp_equilibrium.py` above — those are raw inputs for the planned Bayesian regression, not the abandoned standalone models, which would have been full estimation exercises in their own right.)
- **`export_excel_audit.py`** (built 2026-07-23) — generates `reports/ppp_model_audit.xlsx` (gitignored), a live-formula audit workbook so the whole pipeline can be checked by hand outside Python: `Raw Data` (original series, static) → `PPP Equilibrium` (equilibrium/deviation formulas, base-month dropdown) → `Model Parameters` (Bayesian posterior means from `bayesian_results/primary_breakeven_summary.csv`, hardcoded — **explicitly not re-estimated in Excel**, per user call: "it's not necessary to construct the OLS, just plug the parameters"; standardization mean/stdev *are* real `AVERAGE`/`STDEV.S` formulas, since that's mechanical data prep, not model-fitting) → `Deltas` (differencing, 1-month lag, z-scores) → `Decomposition & Bridge` (contribution/cumulative math plus the lvl0..lvl6 sequential level-bridge from `bayesian_deviation_model.py`, reconstructing the actual PTAX rate exactly — verified independently in a pandas shadow calc, max abs error ~1e-15). All sheets share one date-row backbone (row 2 = 1994-07, row 384 = 2026-06) so the same row number means the same month everywhere. Rerun via `uv run python -c "from analytics.exchange_rate.models.export_excel_audit import run; run()"` — requires `bayesian_deviation_model.run()` to have been run at least once first (reads the saved summary CSV, doesn't refit). **Two bugs fixed same day, both caught by direct user review of the generated file**: (1) the IPCA/CPI index headers said "(base=100)", which is misleading since the sample's first visible row (1994-07) already reads ~916 — the index is genuinely seeded at 100 in 1994-01 (before PTAX data starts and gets joined in), and six months of pre-Real-Plan hyperinflation compound before the row becomes visible; header reworded, README section added explaining this is real history, not a data error. (2) The `Decomposition & Bridge` sheet used to share the full 383-row backbone, leaving rows 3-164 blank before its actual data started at row 165 — looked entirely blank on open; fixed by giving that sheet its own sequential row numbering (data starts row 3) while formulas still correctly reference the aligned backbone row on other sheets via a separate `src` row variable. **Not yet updated**: this workbook still reflects the original with-intercept `primary_breakeven` spec — the `primary_no_alpha`/`primary_ecm` follow-up tests (see `bayesian_deviation_model.py` above) aren't reflected here.

### State-space equilibrium model

`referencia/state_space_equilibrium_model.md` proposes treating USD/BRL "fair value" as a Kalman-filter unobserved state, with carry/terms-of-trade/fiscal-credibility as pull variables (BEER/NATREX-style). The doc's original build sequence wanted `terms_of_trade_model.py` and `fiscal_credibility_model.py` built as standalone models first — **both abandoned 2026-07-23** (explicit user call, time-constrained) in favor of starting the measurement equation directly from PPP.

An interactive simulator for the doc's 3 toy equations (measurement/transition/deviation) was built 2026-07-22 and lives at `referencia/state_space_simulator.html` (self-contained, open directly in a browser) — also published as a Claude Artifact at `https://claude.ai/code/artifact/40710b8c-8ee7-4769-9c35-02a7e9180d16` for sharing, but the repo copy is now the source of truth for edits. `referencia/ppp_dashboard.html` (see above) is the first real-data companion to it. Rebuilt three times 2026-07-24 in one sitting — see the `state_space_simulator.html` entries under "Reference material" above for the full blow-by-blow (contemporaneous drivers, then PPP-as-equilibrium after a false start, then a Constant π_diff shape after the Trend default produced an unintended quadratic equilibrium path).

- **`models/state_space_model.py`** (built and fit 2026-07-24, "attempt two") — Bayesian estimate of the deviation equation's own dynamics, cast directly from the concept note's equations, with **Y = the nominal exchange rate (ptax)**, not REER — explicit user call, and it also resolves the sign-convention thread below since `ppp_equilibrium.compute_equilibrium()` is already nominal-anchored. Equilibrium's own noise (η) is fixed at zero for this first build (explicit user call, avoiding the "pile-up problem" where η routinely collapses to zero in finite samples for this model class) — meaning equilibrium(t) is exactly `ppp_equilibrium.compute_equilibrium()`, not filtered, and deviation(t) = `compute_deviation()` is directly observed, not latent. **This is therefore not yet a true two-state Kalman filter** — nothing in it is actually unobserved yet; it's a one-equation Bayesian AR(1)-with-regressors fit (PyMC/NUTS, matching `bayesian_deviation_model.py`'s tooling) on an already-observed series:
  `deviation(t) = α + φ·deviation(t−1) + β_carry·z(Δcarry(t)) + β_tot·z(Δtot(t)) + β_breakeven·z(Δbreakeven(t)) + β_fiscal·z(Δfiscal(t)) + ε(t)`, φ bounded to (0,1) via a Uniform prior (imposes stationarity a priori), deviation_lag1 kept in native units (not standardized) for the same reason `bayesian_deviation_model.py`'s `raw_cols` argument documents.
  Two deliberate departures from `bayesian_deviation_model.py`: (1) deviation(t) enters as a level regressed on its own lag, replacing attempt one's differenced-plus-separate-ECM-spec patchwork with one coherent equation; (2) the four channels enter **contemporaneously** (Δchannel(t), no extra lag) — re-reading `bayesian_deviation_model.py`'s `build_deltas()` while building this module surfaced that it actually lags its deltas by one *additional* month (`.diff().shift(1)`, so Δchannel(t−1) predicts delta_dev(t)) despite the "first differences uniformly" framing — exactly the lagged-not-contemporaneous specification the `state_space_simulator.html` discussion argued against that same day. Attempt one itself is left as-is (already fit and documented before this was noticed).
  **Result** (n=219, 2008-01→2026-04, r-hat=1.00 throughout, no divergences): φ=0.992 (94% HDI [0.982, 1.000]) — implied half-life median 94 months but with an enormous HDI ([33.5, 1246.1] months), the classic near-unit-root symptom where small differences in φ near 1 imply wildly different half-lives; treat the half-life figure as unreliable, not just "different from" the ECM follow-up's ~44-month estimate. α=0.331, HDI crosses zero — no longer a clearly nonzero drift. **Switching from lagged to contemporaneous timing substantially reshuffled which channels carry signal**, not just strengthened them: carry now comes back reliably negative (P<0 not shown but HDI excludes zero: [-0.868, -0.048], the "wrong sign vs UIP" reading, matching `uip_model.py`/`carry_model.py` rather than attempt one's own "no stable sign" finding); terms-of-trade — attempt one's *most* reliable channel (P≈0.82-0.83) — now shows **no signal at all** (P(β<0)=0.511, a coin flip); breakeven leans toward the theoretically expected sign (P=0.811, up from attempt one's unresolved "wrong sign" puzzle, though still short of the 94%-HDI bar); fiscal risk is now extremely strong and certain (β=3.086, P=1.000, vs. attempt one's 0.94). Read honestly: attempt one's channel rankings were partly an artifact of the lagged specification, not settled facts — this doesn't mean attempt two is "more correct" in every particular, only that the two specifications disagree in specific, informative ways worth holding onto rather than picking whichever confirms a prior.
  **Next step, not yet attempted**: let η be freely estimated instead of fixed at zero — only then does this become a genuine two-state Kalman filter (equilibrium blending the raw PPP signal with what the observed rate itself implies), which is the actual point of doing this in state-space form rather than as a single regression.
  **α kept, no-intercept tested directly (2026-07-24, user request "can we remove the drift at all?")**: theoretically motivated, not just a robustness check — if PPP already captures the systematic drift, deviation should be mean-zero long-run, so a nonzero α here is an odd thing for a stationary error term to have. `fit_no_alpha_spec()` refits with α forced to 0 and compares directly against the primary spec: the four betas are essentially unchanged (3rd/4th decimal), but φ shifted higher (0.992→0.994) and the implied half-life jumped (94→143 months, even wider HDI) — the drift didn't disappear, it relocated into φ, the same underlying phenomenon `bayesian_deviation_model.py`'s own no-intercept test found (drift relocates, not vanishes), just landing in a different parameter here. **Decision: keep α** — the channel estimates are robust either way, and removing it doesn't improve anything, just makes the already-fragile half-life estimate more extreme.
  **`fit_kalman()` — the real two-state Kalman filter (2026-07-24, "let's unleash the eta error")**: η now freely estimated (state: eq(t), dev(t); measurement exact, y(t)=eq(t)+dev(t), R=0; identification comes from the two states' differing dynamics — eq is a pure random walk driven by the real BR−US log inflation differential pi_diff(t), dev is the same AR(1)-with-channels as before). Hand-rolled via `pytensor.scan` (no ready-made state-space package fits this shape; `pymc_extras` isn't installed) — validated against the η=0 baseline first (σ_η→0 reproduces `compute_equilibrium()`/`compute_deviation()` to ~1e-9) before being trusted. Fit cleanly: r-hat=1.00 throughout, 0 divergences, ~39 minutes wall-clock (scan-gradient NUTS is much slower than the vectorized regressions; PyTensor also warned no BLAS is linked, degrading linear-algebra ops further — a real speed factor, not a correctness one). **Headline result: σ_η did NOT collapse to zero** (P(σ_η<0.1)=0.007, mean 2.49, HDI [0.76,3.49]) — the "pile-up problem" flagged as the main risk of this build didn't materialize. Consequences: φ came down from ~0.992 to 0.970, σ (deviation noise) roughly halved (3.15→1.55, since equilibrium now absorbs noise that previously had nowhere to go but the residual), and the implied half-life dropped from a median of 94 months to **27 months** — much closer to Rogoff's classic 3–5 year PPP-puzzle range, and a strong sign the η=0 model was inflating φ by forcing deviation to explain equilibrium's own wiggles too. Carry weakened and its HDI now touches zero (was reliably negative); terms-of-trade still shows no signal; breakeven and fiscal are essentially unchanged. Because the measurement is exact, `eq_filtered(t)+dev_filtered(t)` sums EXACTLY to the actual rate on every posterior draw (verified ~1e-14, not just on average) — a "fitted vs actual" chart would be a tautology, so the dashboard tab instead compares the filter's eq/dev *split* against the rigid η=0 split. That comparison surfaces a substantively large finding: at the latest month, filtered equilibrium is **R$5.18** vs. the deterministic PPP estimate's R$3.48 (actual PTAX: R$4.99) — the model reattributes much of what looked like "BRL deviation from PPP" into "PPP equilibrium itself has shifted," averaging ~R$0.70 higher than the deterministic path across the sample. Added as a 4th dashboard tab, "Kalman Filter (η free)" (`build_kalman_dashboard_payload()`), mirroring the other tabs' structure minus a historical-fit chart (tautological here) — has the eq/dev-split comparison charts instead. `render_dashboard()` now builds and injects all four payloads (`/*KALMAN_DATA*/` added to `ppp_equilibrium.render()`'s signature).
  **Decomposition added same day** (user noticed it was missing vs. the eta=0 tab): `dev_filtered(t)` isn't just alpha+phi*dev(t-1)+channels the way the eta=0 tab's observed deviation was — it also carries the Kalman filter's own per-period UPDATE (new information the observed rate revealed beyond the deterministic prediction), which isn't attributable to any channel. That update flow is backed out directly from what's already in the saved trace (`dev_filtered(t) - [phi*dev_filtered(t-1) + alpha + channels]`, no need to re-run the filter or have saved the Kalman gain/innovation separately) and given its own bucket alongside the channels. Done **per posterior draw, then averaged** — not at plug-in posterior-mean parameters like the eta=0 tab — because the Kalman recursion is nonlinear in phi/sigma_eta/sigma_eps, so a mean-parameter re-run would NOT exactly reconstruct `dev_filtered`'s true posterior mean (the eta=0 tab's plug-in shortcut only worked because that model was linear in the parameters given fixed regressors). Verified to reconstruct `dev_filtered`'s mean to ~1e-13 pre-rounding. The nominal bridge uses the FILTERED equilibrium (not the deterministic PPP line) as its base layer — everything else follows the same sequential/multiplicative-conversion logic as the other two decomposition charts.
  **DXY added as a 5th channel (2026-07-24, user request after ingesting a new DXY series)**: `macro_international.cmb_dollar_index.dxy` (ICE US Dollar Index, Yahoo Finance DX-Y.NYB, from 1971) joined into `ppp_equilibrium.load_data()`/`build_payload()` and added to `state_space_model._CHANNELS`. Unlike carry/tot/breakeven/fiscal, DXY is a **global** dollar-strength signal, not bilateral BRL-specific — the hypothesis was that broad EM/dollar co-movement explains part of the deviation the bilateral channels miss. Refit (n=219 unchanged — DXY's own coverage starts in 1971, well before the sample's real binding constraint, breakeven/fiscal from 2006–07, so it didn't shrink the sample): clean convergence (r-hat=1.00 throughout, 41s wall-clock — this is the cheap regression-style spec, not the ~39-minute Kalman fit), and **DXY comes in strong and unambiguous**: β=+1.031, 94% HDI [0.601, 1.469], P(β>0)=1.000, matching the expected sign (global dollar strength → BRL depreciates too). Fiscal stayed similarly dominant (β=2.635, P=1.000); carry/tot/breakeven were not materially changed in sign or size. φ ticked up further (0.993, vs. 0.992 before), pushing the already-fragile implied half-life even higher (median 111 months, HDI [38, 1669]) — same near-unit-root caveat as before, now more pronounced.
  Scoped deliberately to the eta=0 primary spec only, **not** the Kalman filter tab — `fit_kalman()` costs ~39 minutes per run, a real cost not worth spending without being asked. `_CHANNELS` (module default) now includes `dxy`; `_KALMAN_CHANNELS` (frozen at the original 4) keeps `build_kalman_dashboard_payload()` reading correctly against its existing saved trace (`kalman_idata.nc`) without a refit or KeyError. `build_regressors()`/`build_kf_series()`/`fit_kalman()`/`build_dashboard_payload()`/`build_kalman_dashboard_payload()` all gained a `channels` parameter to support this split; `render_dashboard()` passes `channels=_KALMAN_CHANNELS` explicitly for the Kalman payload. The nominal-bridge (`level_decomposition`) construction in both `build_dashboard_payload()` and `build_kalman_dashboard_payload()` was generalized from a hardcoded 4-line multiplicative chain (`lvl_3 = lvl_2 * exp(delta_carry/100)`, etc.) into a loop over `delta_cols`, so it now works for either channel set without naming individual channels — reverified to reconstruct the actual PTAX rate to the same ~2e-4 precision as before the refactor, for both tabs. Dashboard template (`ppp_dashboard_template.html`) updated to match: a 6th raw-data panel (`dxyChart`) on tab 1, and `delta_dxy`/`beta_delta_dxy` entries added to the State-Space tab's param labels, decomposition/level-bridge color+label+order maps, and coefficient-plot maps (color `#FBC852`, the LIS palette's unused "amarelo" — every other color in that chart set was already taken). The Kalman tab's own maps were deliberately left untouched (still 4-channel). If/when the Kalman tab should catch up: `fit_kalman(channels=_CHANNELS)` then `build_kalman_dashboard_payload(channels=_CHANNELS)` (also update `render_dashboard()`'s call to match).
  **Dashboard, same day**: `ppp_dashboard.html` gained a third tab, "State-Space (Attempt Two)" — same `/*MARKER*/` + `str.replace()` pattern, now three: `/*PPP_DATA*/`, `/*BAYES_DATA*/`, `/*STATESPACE_DATA*/` (`ppp_equilibrium.render()` signature extended accordingly). Mirrors the Bayesian Model tab's structure (equation cards, diagnostics table, historical fit, decomposition, posteriors) with two differences: the historical fit is a direct per-draw reconstruction (`alpha + phi*deviation_lag1 + betas·z`) rather than an anchor+cumsum trick, since deviation(t-1) is a real observed regressor here, not reconstructed from differences; and there's a second historical-fit chart in nominal PTAX terms (`equilibrium(t)·exp(fitted_deviation(t)/100)` vs. actual), reflecting the whole point of this build being Y=nominal. `state_space_model.build_dashboard_payload()` + `render_dashboard()` added, mirroring `bayesian_deviation_model.py`'s own — verified the nominal decomposition reconstructs the actual PTAX rate to ~1e-4 before wiring the HTML. `state_space_model.py`'s own `run()` must be called at least once first (saves to `state_space_results/`, gitignored, same convention as `bayesian_results/`). JS syntax checked with `node --check` on the extracted script block; actual browser rendering not yet visually confirmed — do that before trusting this fully.

  **`models/beer_model.py` — "BEER Model (Levels)", 5th dashboard tab (2026-07-24, user-specified equation)**: a genuinely different spec from the other two model tabs, both structurally: `deviation(t) = alpha + sum(beta_c * z(channel_c(t))) + eps(t)` — channels enter as their own contemporaneous **LEVEL** (standardized), not a delta, and there's **no autoregressive term at all** (no phi, no deviation(t-1)) — a plain static multivariate regression, classic BEER/"pull factors" reading. alpha is a deliberate addition beyond the literal equation as given (which has no constant) — same rationale as the other tabs' alpha, flagged explicitly in the tab's own callout. Two new channels only used here: `trade_pct_gdp`/`ca_pct_gdp` (trade balance and current account, both 12m-trailing-sum-over-12m-trailing-sum-GDP-in-USD %, reusing `generate_report.py`'s own "% PIB" convention exactly) — added to `ppp_equilibrium.load_data()`/`build_payload()` alongside the other five, so tab 1 also gained two raw-data panels (`trade_pct_gdp`/`ca_pct_gdp` charts). A prior throwaway delta-form test of these two channels (scratchpad, not committed) found no signal against the AR(1) spec; this LEVELS-only, no-AR spec is a different question, so that null result doesn't carry over.
  **Result** (n=220 — one more than the delta-based specs, since there's no `.diff()` to lose the first observation to; 2007-12→2026-04, r-hat=1.00 throughout, 42s wall-clock): striking, and mostly cleanly significant. beta_carry=-8.35 (94% HDI [-10.16,-6.48], decisively negative, same "wrong sign vs UIP" direction as the delta specs but now unambiguous in levels form); beta_tot=+1.41 (HDI crosses zero, no signal); beta_breakeven=+1.90 (HDI [0.22,3.67], expected sign); beta_fiscal=+6.61 (HDI [4.86,8.48], expected sign, strong); beta_dxy=+13.88 (HDI [11.83,15.72], expected sign, strongest of all seven); beta_trade_pct_gdp=+7.55 (HDI [5.16,10.16], significant but the WRONG sign vs. the naive expectation that a wider trade surplus should mean a stronger currency, i.e. lower deviation); beta_ca_pct_gdp=-2.88 (HDI [-5.11,-0.93], significant and matching the expected sign — a smaller CA deficit predicts lower deviation). alpha=14.5 (HDI [13.1,15.9]) absorbs the model's unconditional mean deviation, unsurprising with no reversion mechanism of its own.
  **Read with real caution, flagged directly in the tab's own callout, not just here:** a levels-on-levels regression with no AR term and no explicit time trend can't distinguish a genuine pull-factor relationship from two persistent series that happened to trend together over the same 18-year window — sharper spurious-regression risk than the other tabs' delta-based specs, and the likely explanation for `trade_pct_gdp`'s counterintuitive sign. Trade balance and current account are themselves highly correlated (trade is a large component of CA) — even where both come out individually significant, that should read as "the model can't fully separate these two," not two clean independent effects.
  Decomposition here needed no phi-discounted recursive unrolling (unlike the other two tabs) — since deviation(t) doesn't depend on deviation(t-1), the regression IS the decomposition already: each channel's contribution at time t is simply `beta_c * z_c(t)`, summed with alpha and the residual. Both the log-% and nominal-bridge reconstructions verified exact to ~3e-4 (same rounding precision as every other tab). `beer_model.build_dashboard_payload()` + `state_space_model.render_dashboard()` extended to build and pass a 5th payload (`/*BEER_DATA*/`). Results saved to `beer_results/` (gitignored, same convention as `bayesian_results/`/`state_space_results/`). JS syntax checked with `node --check`; actual browser rendering not yet visually confirmed.

  **Shrinkage test, same day (user request: "can we run the model with a shrinkage?")**: `beer_model.fit_shrinkage()` replaces the flat `Normal(0,2)` betas with a REGULARIZED HORSEHOE prior (Piironen & Vehtari 2017) — non-centered (`beta_c = tau * lambda_tilde_c * z_c`), with a capped local-shrinkage `lambda_c` per channel and a shared global `tau` set via `tau0 = p0/(p-p0) * sigma_guess/sqrt(n)`. Tried at `p0=3` and `p0=5` (both `target_accept=0.99`, `tune=3000` — horseshoe posteriors have a well-known funnel geometry): both gave essentially the SAME result — `tau`'s posterior came back huge and heavy-tailed (mean 135–155, sd >1400, 94% HDI spanning [1–2, 260–325]) in both cases, meaning the prior found no sparse subset to shrink toward, regardless of the `p0` guess. Coefficients came out slightly LARGER under shrinkage than under the flat prior, not smaller. **Read: this isn't a failed/misconfigured shrinkage run — it's informative on its own.** The data support non-trivial signal in most of the 7 channels (everything except `tot`), so there's no small "real" subset for the horseshoe to find; shrinkage doesn't materially change the flat-prior story for this model. Not wired into the dashboard — a comparison-only exploration (`fit_shrinkage()` stays in the module for reuse, results saved under `beer_hs`/`beer_hs_p5` labels in `beer_results/`).

  **Channel elimination rounds, same day, leading to a "core 4" spec**: a series of direct comparisons (`fit(channels=...)`, no dashboard changes until the last step) tested which of the 7 channels actually survive removal of others:
  - Dropped `tot` + `trade_pct_gdp`, kept `ca_pct_gdp` (5-channel): `beta_ca_pct_gdp` **FLIPPED SIGN** (from -2.88, matching theory, to +1.60, HDI barely excluding zero) — direct evidence CA's earlier "correct sign" wasn't an independent finding, just an artifact of being estimated jointly with the collinear `trade_pct_gdp` (trade is a large component of CA).
  - Dropped `tot` + `ca_pct_gdp` instead, kept `trade_pct_gdp` (5-channel): `beta_trade_pct_gdp` stayed positive and significant (+5.24, HDI [+3.51,+6.98], P>0=1.000) whether or not CA was present — more robust than CA, though still the "wrong sign" vs. the naive expectation (plausible reading: reverse causality/simultaneity in a commodity exporter, where a weak currency itself often widens the trade surplus rather than the other way around). This version also fit slightly better (sigma 11.80 vs. 12.14 for the CA version).
  - Final step, by direct user request: dropped `trade_pct_gdp` too, leaving **`_CHANNELS_CORE4 = [carry, breakeven, fiscal, dxy]`** — the four channels that held up most consistently across every swap tested. Whole-sample fit (label `beer_core4`, n=222, 2007-12→2026-06, r-hat=1.00): alpha=14.71, beta_carry=-8.90 (HDI [-10.67,-6.99]), beta_breakeven=+2.31 (HDI [0.47,4.18]), beta_fiscal=+4.27 (HDI [2.61,5.87]), beta_dxy=+17.87 (HDI [16.20,19.55]) — all four cleanly significant in whole-sample terms.

  **`rolling_fit()` + "Rolling Window (Core 4)", 6th dashboard tab, same day (user request: "run the model with a five year rolling horizont, I want to see if there is some change in the parameters in time")**: OLS+HAC (maxlags=3) per 60-month (5y) window, monthly step, 163 windows (2012-11→2026-06) — explicit user choice over a full Bayesian re-fit per window, which would have taken well over an hour for the same substantive answer. Channels standardized ONCE on the full sample's own mean/std (not per-window) so an apparent drift in a beta reflects a real change in the relationship, not the window's own shifting mean/std. **Result: real, large parameter instability, not noise** — R² alone swings from 0.96 (2013-2018 windows) down to 0.24 (2022 window) and back to ~0.45 today. DXY (the whole-sample fit's strongest, most significant channel) swings from ~+16 (2016-2018) to ~-10 (2022) to near zero today — its whole-sample significance is essentially an average across a period where it worked strongly and periods where it didn't work or worked backwards. Carry and breakeven both change sign at points too; fiscal is the most stable for most of the sample but fades to insignificance in the most recent window. **Practical read: the whole-sample coefficients describe an average relationship over 2008-2026, not a stable structural one** — treat the most recent 3-5 years as more informative than the full-sample average for anything forward-looking.
  First built as a standalone Claude Artifact (self-contained HTML, vanilla canvas — no CDN dependencies, since Artifacts run under a strict CSP that blocks them). **User couldn't see the artifact** (rendering/access issue, cause not diagnosed) — redirected into the dashboard proper instead, which isn't CSP-constrained and already has Chart.js loaded, so the tab reuses it like every other tab rather than hand-rolled canvas drawing. Tab has three pieces, all from `beer_model.build_rolling_payload()` (rolling betas + R² + whole-sample reference values) and `beer_model.build_dashboard_payload(label="beer_core4", channels=_CHANNELS_CORE4)` (the core-4 model's own decomposition/level_decomposition, reused as-is): (1) a single "Rolling Coefficient" chart with a dropdown (`<select>`, styled via a new shared `.base-select` CSS class rather than duplicating `select#baseMonth`'s rule) to switch between alpha/carry/breakeven/fiscal/dxy, each with a 95% CI band and a dashed whole-sample reference line; (2) an always-visible R² line chart; (3) the nominal (BRL/USD) bridge decomposition with a start-month `<select>` (defaults to 2025-01) that slices the SAME whole-sample decomposition arrays from that index forward — a zoom, not a re-based/re-derived decomposition — so a recent period can be read without 2008-2026's full history crowding the chart. `ppp_equilibrium.render()`/`build_dashboard_payload()` (in `beer_model.py`) both extended with a `channels` param to support the core-4 spec alongside the original 7-channel tab; `/*ROLLING_DATA*/` is the 6th and newest template marker. Nominal-bridge reconstruction reverified exact (~2e-4) for the core-4 payload specifically, same as every other tab. JS syntax checked with `node --check`; actual browser rendering not yet visually confirmed.

- **`fx_attribution_model.py`** (built 2026-07-29) — a different kind of model from everything else in this section: not a regression against price/rate series, but qualitative FX commentary from asset-manager monthly letters turned into a numeric time series. For each month, sums how much of the narrative attributes BRL/USD moves to each of nine fixed causal regimes (`fiscal_br`, `monetary_br`, `politics_br`, `global_usd`, `commodities`, `risk_sentiment`, `china_em`, `trade_policy`, `capital_flows` — see `fx_attribution_model.md` for the full taxonomy/boundary table and extraction rules). Sign convention matches every other tab's direction-scoring pattern: +1 = strongly BRL-appreciation-supportive, −1 = strongly BRL-depreciation-driving, scored on the claim's effect **on BRL**, never on the claim's own subject. **Manual-extraction pilot, not an automated pipeline** — each manager's `claims.csv`/`documents.csv` under `fx_attribution_data/<manager>/` is hand-extracted (Claude reading each source letter directly), following the rules in `fx_attribution_model.md`; `fx_attribution_model.py` only covers claims → monthly matrix → Excel export (`export_excel()`, a `Claims`/`Monthly`/`Trends` 3-sheet workbook with two rolling-average line charts) and, as of 2026-07-29, → dashboard payload. Two managers onboarded so far: **Kinea** (124 documents, 101 claims, 2021-05→2026-06 — the pilot, full available archive) and **Verde Asset** (197 documents, 172 claims, 2010-01→2026-05 across six extension rounds — see the `project-fx-attribution-verde` memory for the extension history and the "any single-window finding here gets overturned as the window grows" lesson learned three times over; the sixth round, 2026-07-30, was also the first to delegate the read-and-extract step to parallel subagents instead of Claude reading every letter directly in-session). Framework is manager-agnostic by design (taxonomy/extraction rules/aggregation logic don't change per manager) — onboarding a new manager means only hand-extracting its own `fx_attribution_data/<manager>/` folder, no code changes.
  **Wired into `ppp_dashboard.html` as its own 7th tab, "FX Attribution (Manager Letters)", same day**: `build_manager_payload()`/`build_dashboard_payload()` (new) reuse `aggregate_monthly()`/`rolling_mean()` as-is — same 3-month rolling-average convention as the Excel `Trends` sheet, both the signed (directional) and `|score|` (magnitude/salience) views — and add a manager-keyed `label`/`totals`/full claim list for the tab's own table. `render()`'s signature (`ppp_equilibrium.py`) gained a `fxattr_payload` argument and `/*FXATTR_DATA*/` became the 7th template marker; `state_space_model.render_dashboard()` now also calls `fx_attribution_model.build_dashboard_payload()` and threads it through. The tab itself: a manager `<select>` (auto-populated from whichever manager subfolders actually exist — a newly onboarded manager appears with no template change), a coverage stats row, two multi-line Plotly charts (directional relevance and magnitude, one line per category, click a legend entry to isolate — native Plotly legend-click, no custom isolate logic needed), a grouped bar chart of documents/claims per month, and a claims table (date/category/direction/source/quote, filterable by category, most-recent-first) built via plain string templating with an `escapeHtmlFx()` helper (quotes are free-text extracted from PDFs and could otherwise break the table's HTML). JS syntax checked with `node --check`; **dashboard regeneration itself (`state_space_model.render_dashboard()`) requires a live connection to the internal MySQL host for `load_data()`'s PPP series** — not available in every environment (e.g. a sandboxed agent session off the office LAN) — so run it from a machine with DB access to actually refresh `referencia/ppp_dashboard.html`; the code changes and payload were verified independently (`fx_attribution_model.build_dashboard_payload()` runs standalone, no DB needed, and JSON-serializes cleanly) but browser rendering of the new tab has not yet been visually confirmed.

- **`ridge_deviation_model.py`** (built 2026-07-30) — the "modelo proposto" from a same-day design note: same dependent variable and channel set already established elsewhere (`delta_dev` regressed on each channel's own contemporaneous z-scored delta, reusing `bayesian_deviation_model.py`'s `build_deltas_contemporaneous()`/`_standardize_ext()` with the 2000-01+ reference window as-is), but the estimator swapped for **Ridge (L2 penalty)** instead of OLS/Bayesian, plus re-estimation on a rolling window so the coefficients can track regime change rather than describing one average relationship across calm and crisis periods. Starts with only two channels — **fiscal (CDS) and DXY (global USD)** — by explicit user instruction ("run it with (i) fiscal and (ii) global USD (DXY). We increment later"), not the full 5-channel `primary_contemp` set; `_CHANNELS` is a short module-level list specifically so growing it later is a one-line change. Three design choices, all directly specified in the source note (an image of a written proposal, not re-derived here): (1) L2 not L1 — the goal is stabilizing coefficients between correlated channels, not zeroing any out (collinearity isn't yet the live concern it will be once more channels are added, but the module is built to grow into that); (2) **lambda is chosen by walk-forward temporal cross-validation** (`walk_forward_lambda()`) — fit on an expanding window, score one step ahead (never used to fit that step), walk forward across the whole history, average the OOS squared error per candidate lambda, keep the best — not fixed by hand; (3) **re-estimated on a rolling 60-month window** (`rolling_fit()`), the same window `beer_model.py`'s own `rolling_fit()` uses, at that one fixed lambda (chosen once, globally — re-selecting it inside every one of ~590 windows would let lambda itself drift for reasons unrelated to the coefficients' own stability, the thing actually being tested). No posterior/HDI anywhere in this module — Ridge is a point estimate, not a Bayesian fit, so there's nothing to summarize beyond the coefficient itself, unlike every PyMC-based model elsewhere in this package. Uses `sklearn.linear_model.Ridge` (already a project dependency, not newly added).
  **No live DB access in the building session** — every function was verified against synthetic data instead (a fabricated `ptax`/`ipca_index`/`cpi_index`/`fiscal`/`dxy` panel with a real base month, since `compute_equilibrium()` looks up 1994-07 by label): `build_sample()`, `walk_forward_lambda()`, `fit_whole_sample()`, `rolling_fit()`, and `build_dashboard_payload()` all ran without error, and the decomposition/level-bridge reconstruction matched the actual level/PTAX rate to ~2e-4 (the same rounding-precision bar every other model's decomposition in this file is held to).
  **DB access came back later the same day** — `run()` against real data (2-channel spec, n=222, 2008-01→2026-06): walk-forward CV selected **lambda at the floor of the grid tested (0.01)**, meaning with only fiscal+DXY there wasn't enough collinearity yet for the penalty to matter (OOS MSE was nearly flat across the whole lambda range, 8.523→8.536) — an honest, expected result for a 2-channel start, not a bug. Whole-sample fit: alpha=+0.164, beta_fiscal=+2.620, beta_dxy=+1.082, R²=0.543 — both betas land close to `bayesian_deviation_model.py`'s 5-channel `primary_contemp` spec (fiscal +2.651, dxy +1.030), a good consistency check that the z-scoring/reference-window plumbing matches across modules. `referencia/ppp_dashboard.html`'s `RIDGE_DATA` was updated in place with this real payload (surgical single-line replacement of the `null` placeholder, not a full `render_dashboard()` — see below for why).
  **Wired into `ppp_dashboard.html` as an 8th tab, "Ridge (Regularized, Rolling)", same day**: `build_dashboard_payload()` builds fresh on every call (no saved-trace read-back like the PyMC models — Ridge fits in milliseconds, so there's no minutes-per-run cost to amortize by caching). `render()`'s signature (`ppp_equilibrium.py`) gained a `ridge_payload` argument and `/*RIDGE_DATA*/` became the 8th template marker; `state_space_model.render_dashboard()` now also calls `ridge_deviation_model.build_dashboard_payload()` and threads it through (`ridge_deviation_model.render_dashboard()` itself just delegates to `state_space_model.render_dashboard()`, keeping one canonical entry point rather than a second parallel one). The tab: a lambda-selection chart (OOS MSE vs. lambda, log-x, **not** run through the shared `plotlyRenderAndBind()`/`plotlyBaseLayout()` helpers since those assume a date x-axis with a rangeselector — same category-of-exception as the coefficient/forest bar charts and posterior histograms elsewhere in this file, done here via a direct `Plotly.newPlot()` call instead), a rolling-coefficient chart with a dropdown (alpha + one entry per channel) showing the point estimate against the whole-sample reference line — **no confidence band**, unlike the BEER rolling tab's OLS+HAC bands, since a penalized estimator's standard error isn't as settled a construct and wasn't asked for — an R² chart, and the PPP-to-PTAX decomposition bridge (whole-sample fit). Verified two ways beyond `node --check` syntax validation: (1) the extracted tab IIFE was executed directly in Node against a stubbed `document`/`Plotly` and a real `build_dashboard_payload()` output — the "no data" branch didn't fire, the param `<select>` populated with the expected number of options, all 4 charts actually called their render function, and switching the dropdown didn't throw; (2) the reconstruction identity above. **Actual browser rendering has not been visually confirmed** — same standing caveat as every other tab in this file.
  **`referencia/ppp_dashboard.html` updated by surgical single-line replacement, not a full `render_dashboard()`, both times real data was fetched** — a full run hits a pre-existing, unrelated bug: `build_kalman_dashboard_payload()`'s saved trace (`state_space_results/kalman_idata.nc`) was fit against an older, shorter sample (219 months) than `load_data()` now returns, so `post["eq_filtered"].values.reshape(-1, n)` throws a `ValueError` (array size 1,752,000 doesn't factor into the current `n`). Direct user instruction: "Focus just on the Ridge tab, let the other untouched" — so instead of `render_dashboard()` (which rebuilds and overwrites all eight payloads), only the `const RIDGE_DATA = ...;` line was replaced in place (regex-matched, single occurrence asserted, JS re-syntax-checked after). The Kalman staleness bug itself is unfixed and out of scope here — it needs either a `fit_kalman()` refit (~39 minutes, see `state_space_model.py`) or a code fix to align the trace's sample window with the current one; flagged for a future session, not silently worked around.
  **Grown from 2 to 6 channels, same day, direct user instruction** ("introduce the carry metrics: carry, relative carry, carry-vol, relative carry-vol" — added together, not one at a time, per the user's own explicit choice after being asked and agreeing that a joint fit is what Ridge is actually for: letting overlapping/collinear channels share credit rather than forcing a pick, and surfacing interactions a one-at-a-time ablation would hide, like `bayesian_deviation_model.py`'s earlier finding that `relative_carry` sharpens `carry`'s own estimate once fit alongside it). `_CHANNELS` extended to `["fiscal", "dxy", "carry", "relative_carry", "carry_vol", "relative_carry_vol"]` — no other code changes needed, `build_deltas_contemporaneous()` already computed all six `delta_*` columns. Dashboard's `CHANNEL_COLORS_RIDGE`/`CHANNEL_LABELS_RIDGE`/`CHANNEL_ORDER_RIDGE`/`PARAM_LABELS_RIDGE` extended to match, reusing the same hex colors tab 1's channel dropdown already assigns to `relative_carry`/`carry_vol`/`relative_carry_vol` (`#6E8B3D`/`#9B5DE5`/`#F15BB5`) so the same variable reads as the same color across tabs.
  **Refit result (n=222, 2008-01→2026-06): walk-forward CV now selects lambda=3.162, off the grid floor** — direct confirmation of the design rationale: with the four correlated carry variants added, there's now real collinearity for the penalty to correct, unlike the 2-channel run above where lambda sat at the floor with an almost-flat OOS-MSE curve. Whole-sample fit: alpha=+0.205, beta_fiscal=+2.536, beta_dxy=+1.075 (both essentially unchanged from the 2-channel run), beta_carry=−0.757 (negative, the familiar "wrong sign vs. UIP" reading also found in `uip_model.py`/`carry_model.py`/`bayesian_deviation_model.py`), beta_relative_carry=+0.593, beta_carry_vol=−0.941 (new — carry-to-volatility hadn't been regressed against the deviation anywhere before, only plotted as a raw tab-1 channel), beta_relative_carry_vol=+0.128, R²=0.577 (up from 0.543 with 2 channels — a modest gain, not a large one). Rolling-window read (163 windows): **fiscal and dxy are the stable core** — fiscal never crosses zero across any window (range +1.40 to +3.28), dxy likewise (range +0.53 to +1.77); carry is mostly negative but not uniformly (mean −0.62, but a max of +0.59 — some windows do flip sign); carry_vol is mostly negative too (mean −0.47, range −1.50 to +0.27); relative_carry and relative_carry_vol read as close to noise around zero across the rolling history (means +0.28/−0.08, both with ranges comfortably spanning both signs) — consistent with `state_space_model.py`'s own earlier finding that `relative_carry`'s HDI crosses zero. Read honestly: this is a single afternoon's rolling read, not a stress-tested finding — the "single-window carry result gets overturned as more data/channels are added" pattern has recurred multiple times elsewhere in this file, so treat `carry_vol`'s new negative coefficient as a lead to watch, not a settled result.
  **Grown from 6 to 8 channels, same day, direct user request** ("add: Breakeven de anchor and terms of trade and run it"): `breakeven_gap` (the breakeven-minus-CMN-target de-anchoring gap) and `tot` (terms of trade) added to `_CHANNELS` — no changes needed to `build_deltas_contemporaneous()`, both `delta_breakeven_gap`/`delta_tot` already existed there from `bayesian_deviation_model.py`'s own earlier specs. Dashboard maps extended with the same colors already established for these two channels elsewhere in the file (`breakeven_gap` = `#3A4F72`, `tot` = `#418791`, matching the Bayesian/state-space tabs' own decomposition colors) rather than picking new ones.
  **Refit result (n=220, 2008-01→2026-06 — two months shorter than the 6-channel run, `breakeven_gap`/`tot`'s own coverage constraints): lambda unchanged at 3.162.** Whole-sample fit: alpha=+0.203, beta_fiscal=+2.469, beta_dxy=+1.075, beta_carry=−0.732, beta_relative_carry=+0.559, beta_carry_vol=−0.949, beta_relative_carry_vol=+0.134 (all six essentially unchanged from the 6-channel run), beta_breakeven_gap=+0.144 (small, HDI-crossing-zero territory in every other model that's tested this channel — consistent), beta_tot=−0.071 (small, near zero — matches `state_space_model.py`'s contemporaneous-spec finding of "no signal at all" for tot, rather than `bayesian_deviation_model.py`'s lagged-spec finding of tot as its *most* reliable channel; this model's timing convention is contemporaneous, so the state-space comparison is the relevant one). R²=0.578 — breakeven_gap/tot add essentially nothing to the whole-sample fit. Rolling-window read (161 windows): fiscal/dxy remain the stable core (fiscal range +1.50 to +3.22, dxy range +0.51 to +1.88, neither ever crosses zero); breakeven_gap is mostly positive but not always (mean +0.40, range −0.40 to +1.14); tot stays small and centered near zero throughout (mean −0.08, range −0.31 to +0.29) — no reliable signal in the rolling read either, consistent with the whole-sample point estimate.
  **Base-month digression, same day, no code change**: user asked to re-run with the PPP base month changed to 1999-02. Checked the math first rather than just re-running: `compute_deviation(df, base_month=b)` shifts the level by a constant that depends only on `b`, not on `t` (confirmed empirically on real data — the level difference between base=1994-07 and base=1999-02 is a single constant, −40.8676, std ~1.6e-14 across all 384 months), so `delta_dev(t) = deviation(t) − deviation(t−1)` — the only thing this model ever regresses on — is base-month INVARIANT (the two delta series matched to 6.6e-14, i.e. floating-point noise). Since this Ridge model's dependent variable is the delta, not the level, changing the base month here would have changed nothing about the fit. User pushed back reasonably ("how would that change not matter?") — resolved by distinguishing level (genuinely base-dependent) from month-over-month change (base-invariant by construction, same reason a constant offset cancels out of any first difference) — not implemented, since the base-month choice only affects level/display, not this model's actual regression.
  **Lag structure / impulse decay, same day, direct user request** ("a shock in interest rate have impact a continuing impact over the fx, we do not capture it... how can we do?"): the contemporaneous-only spec above has no persistence mechanism at all — a channel's shock only ever affects the same month's `delta_dev`. Two fixes discussed: (1) a single shared AR term (`phi`, `deviation(t-1)`, the mechanism `state_space_model.py` already uses) or (2) an explicit per-channel lag structure. **Chosen: (2)** — verified by unrolling the AR(1) recursion that a shared `phi` gives every channel the exact SAME decay rate `phi^k` at horizon k (only `beta_c` scales the size, never the speed), which can't represent a real difference in how fast different channels' effects fade. **Lag count: 6 months/channel**, user's choice. **Estimation scope: whole-sample only, no rolling window** — also the user's own call, once the arithmetic was laid out: 8 channels × 6 lags = 48 regressors + alpha, and a 60-month rolling window would leave ~11 degrees of freedom per window (unreliable even under Ridge's penalty), while a window wide enough to be comfortable (150+ months) would only yield a handful of windows across the ~220-month sample anyway.
  `build_lagged_sample()` (new): each channel's z-scored contemporaneous delta (same 2000-01+ reference standardization as everywhere else) is shifted to produce `delta_<channel>_lag0..lag5` — lag0 is identical to the existing contemporaneous column, lag_k is that same series shifted k months. `fit_lag_structure()` reuses `walk_forward_lambda()`/`fit_whole_sample()` completely unchanged (both were already written generically over an arbitrary `delta_cols` list) — only `min_train` was raised from 36 to `_LAG_MIN_TRAIN=72` for this wider design, since the first few folds of a 36-month floor would be fitting more parameters (49) than observations. `build_lag_dashboard_payload()` reshapes the flat beta dict into one 6-length array per channel for the tab's chart.
  **Result (n=215, 2008-08→2026-06, wall-clock 13s): walk-forward CV selects lambda=56.23 — an order of magnitude higher than the contemporaneous 8-channel spec's 3.16**, as expected: 6 lags of the same channel are far more collinear with each other than 8 different channels are, so a much stronger penalty is needed to stabilize them. R²=0.605 (up from 0.578 contemporaneous-only — a modest gain from letting effects spread across months). **The decay shapes differ genuinely by channel, not just in size**: fiscal (lag0=+1.944, then +0.087/+0.054/+0.109/−0.37/+0.109) and dxy (lag0=+0.987, then near-zero/noisy) both concentrate almost their entire effect in the contemporaneous month, with nothing resembling a real multi-month tail — consistent with the AR(1) tabs' own finding that these two channels are the stable, reliable core. **carry looks structurally different**: lag0=+0.074 (much smaller than the −0.732 contemporaneous-only estimate) with small negative coefficients spread fairly evenly across lags 1-5 (−0.1, −0.062, −0.178, +0.003, −0.132) — i.e. carry's relationship with the deviation reads as a slower, more distributed effect once given the chance to spread across months, rather than the one-shot hit the contemporaneous-only spec forced it into. carry_vol shows a broadly similar spread-out negative pattern (lag0=−0.494, then −0.105/−0.179/−0.183/+0.165/−0.181). breakeven_gap/tot stay small and mostly noise-like at every lag, consistent with their near-zero contemporaneous betas. **Read honestly**: this is one whole-sample fit, not validated out-of-sample per lag or checked for stability the way the rolling tabs check the contemporaneous spec — carry's spread-out pattern is a genuinely interesting lead (it directly answers the user's original question, that some channels' effects plausibly do persist beyond the month they occur) but should be treated as a hypothesis this single fit surfaced, not a settled finding.
  **Wired into the Ridge tab as a new "Impulse Decay by Channel (Lag Structure)" section, same day**: `build_dashboard_payload()` now includes a `lag_structure` key (built via `build_lag_dashboard_payload()`) alongside the existing rolling/decomposition data — no new template marker or `render()` signature change needed, since it's nested inside the existing `RIDGE_DATA` payload. New chart (`ridgeLagChart`): a channel `<select>` plus a bar chart of that channel's coefficient at each lag (0-5 months after the shock) — x is a lag count, not a date, so (like the lambda-selection chart above it) this deliberately skips `plotlyRenderAndBind()`/`plotlyBaseLayout()` and calls `Plotly.newPlot()` directly. A new stats row above it reports the lag-fit's own lambda/R²/n and parameter count (spelling out why this is whole-sample-only). Verified the same two ways as the rest of the tab: JS syntax (`node --check`) and the extracted IIFE executed against the real payload in a stubbed DOM (lag-channel `<select>` populated with exactly 8 options — no "alpha" entry, since alpha isn't per-channel here — and the new chart's `Plotly.newPlot` call confirmed to fire).
  **Lag-depth walk-forward comparison, same day, direct user follow-up ("do we have some lag to shrink?"): overturns the "read as a lead" framing above — the lag structure does NOT survive out-of-sample validation.** `compare_lag_depths()` (new) refits the lag structure at every depth 1-6, each with its OWN walk-forward-selected lambda, with every depth's sample restricted to the deepest depth's own date range first so all depths are scored on the identical held-out months (a shallower depth naturally has a longer sample/different fold set otherwise, which would make the comparison unfair). **Result: OOS MSE rises monotonically with every added lag** (depth1=8.28 → depth2=9.06 → depth3=9.53 → depth4=9.86 → depth5=9.90 → depth6=10.33), while whole-sample R² mostly climbs (0.580→0.583→0.591→0.594→0.610→0.605) — the textbook overfitting signature: more parameters improve the in-sample fit almost mechanically, but genuinely predict WORSE one step ahead. Depth 1 (no lags at all, i.e. the plain contemporaneous 8-channel spec) is the best of the six by the honest metric. Cross-checked against coefficient magnitudes: mean |beta| by lag position in the depth-6 fit is 0.53 at lag0 vs. 0.075-0.147 at lags 1-5 (small, roughly flat, no clean decay shape) — consistent with "lag0 carries the real signal, lags 1-5 mostly fit noise," including for carry/carry_vol, whose earlier "spread evenly across lags" pattern does not survive this OOS check. **The shipped "Impulse Decay by Channel" tab still shows the uncaveated depth-6 fit** — this OOS finding is documented here but the tab itself has not yet been updated to reflect it (paused pending a decision on how to handle the tab, see below).
  **AR(1)-on-the-dependent-variable test, same day, direct user follow-up ("Test only with a AR(1) for the dependent variable")**: tests persistence option (1) from the original lag-structure discussion — a single shared AR term — properly, instead of option (2) (the per-channel lag structure just shown to overfit). `build_ar1_sample()` (new): `delta_dev(t)` regressed on its own lag `delta_dev(t-1)` (kept in RAW units, not z-scored — same `raw_cols` rationale as `bayesian_deviation_model.py`'s `deviation_lag1`, so phi stays directly interpretable) plus all 8 channels' contemporaneous z-scored deltas (no per-channel lags). **Result, scored on the same aligned OOS comparison as the lag-depth table above: OOS MSE=8.19, BETTER than both the contemporaneous-only spec (8.28) and the depth-6 lag structure (10.33)** — a single extra parameter that genuinely improves one-step-ahead prediction, unlike 40 extra lag parameters that hurt it. Whole-sample fit: alpha=+0.248, phi (`delta_dev_lag1`)=**−0.080** (small, negative — a month that moves more than usual tends to see a modest partial reversal the next month, independent of any channel), the other 8 channel betas essentially unchanged from the contemporaneous-only fit (fiscal +2.63, dxy +1.00, carry −0.60, relative_carry +0.42, carry_vol −0.93, relative_carry_vol +0.07, breakeven_gap +0.06, tot −0.09). R²=0.586 (n=215, same aligned sample as the lag-depth table). **Read: small in magnitude, but the first persistence mechanism tested in this model that actually survives out-of-sample validation, at a fraction of the lag structure's cost.**
  **Tab updated with the AR(1) model, same day, direct user instruction ("Update the tab with the AR(1) model")**: `build_dashboard_payload()` switched from `build_sample()` to `build_ar1_sample()` as the tab's actual model — every chart on the tab (λ selection, whole-sample stats, rolling coefficients, decomposition/level-bridge) now reflects the 9-parameter AR(1) spec (8 channels + `delta_dev_lag1`), not the old plain-8-channel one. `delta_cols` inside `build_dashboard_payload()` is now just an alias for `build_ar1_sample()`'s `reg_cols` — every downstream line (contributions, cumulative decomposition, rolling betas) was already written generically over that list, so `delta_dev_lag1` falls into the decomposition/level-bridge as its own bucket with no special-cased code. The "Impulse Decay by Channel (Lag Structure)" section (HTML `model-section` + `ridgeLagStatsRow`/`ridgeLagChannelSelect`/`ridgeLagChart`) was removed outright from both `ppp_dashboard_template.html` and `referencia/ppp_dashboard.html` — shown to overfit OOS, so keeping it displayed uncaveated would misrepresent the finding; `build_lag_dashboard_payload()`/`fit_lag_structure()`/`build_lagged_sample()`/`compare_lag_depths()` all stay in `ridge_deviation_model.py` for reproducibility, just no longer called from `build_dashboard_payload()`. A 4th intro callout was added explaining the swap (why AR(1) replaced the lag structure, in one paragraph) and `CHANNEL_ORDER_RIDGE`/`CHANNEL_LABELS_RIDGE`/`CHANNEL_COLORS_RIDGE`/`PARAM_LABELS_RIDGE` all gained a `delta_dev_lag1` entry (label "AR(1) — Δdev(t-1)", color `#8C6A5D`, a muted brown not used anywhere else in this file, since this term isn't a fundamental channel and shouldn't visually read as one). No new template marker needed — same `RIDGE_DATA` payload shape, just one more entry in `whole_sample.beta`/`rolling.channels`/`level_decomposition`. **Refreshed real-data result (n=220, 2008-01→2026-04): λ=3.162 (same as the plain 8-channel spec — the AR term doesn't meaningfully change how much collinearity Ridge has to correct for), alpha=+0.223, phi(`delta_dev_lag1`)=−0.075 (consistent with the −0.080 found on the smaller aligned comparison sample), other 8 betas essentially unchanged (fiscal +2.51, dxy +1.03, carry −0.66, relative_carry +0.50, carry_vol −1.06, relative_carry_vol +0.17, breakeven_gap +0.14, tot −0.10), R²=0.583.** Verified the same two ways as every other change to this tab: `node --check` on both files (accounting for the shipped file's CRLF line endings, which broke the harness's naive `;\n` marker search until fixed to check `;\r\n` first) and the extracted IIFE executed against the real, currently-embedded payload in a stubbed DOM — content shown, exactly 10 param-select options (alpha + 8 channels + AR(1) term), all 4 remaining charts fire (`ridgeLambdaChart`/`ridgeParamChart`/`ridgeR2Chart`/`ridgeDecompChart`), `ridgeLagChart` confirmed to NOT fire, switching to `delta_dev_lag1` doesn't throw. Decomposition/level-bridge reconstruction re-verified to ~3e-4, same bar as every other tab. **Actual browser rendering not yet visually confirmed** — same standing caveat as the rest of this file.
  **Level-space variant, same day, direct user request ("Let's run the model in level not difference and see what we have")**: `build_level_sample()`/`run_level_variant()` (new) swap the dependent variable from `delta_dev(t)` to `deviation(t)` itself, and every channel from its z-scored delta to its own z-scored LEVEL (`df[c]`) — the same channel set, just untransformed. `walk_forward_lambda()`/`fit_whole_sample()`/`rolling_fit()` all gained an optional `y_col` parameter (default `"delta_dev"`, every prior call site unaffected) so they could be reused as-is rather than duplicated for this one variant. **Result (n=221, whole-sample): R²=0.871 — dramatically higher than any delta-space spec today (0.578-0.605)** — betas also much larger (fiscal +4.99, dxy +17.28, carry −13.65, relative_carry +2.91, carry_vol −5.59, relative_carry_vol +2.63, breakeven_gap +7.95, tot +1.75), same sign pattern and comparable order of magnitude to `beer_model.py`'s own earlier static-level regression (dxy strongest positive, carry strongest negative there too). **Read with the strongest caveat in this file, not a genuine improvement**: `bayesian_deviation_model.py`'s own pre-registered ADF/KPSS check found `deviation`, `carry`, and `tot` all test as I(1) (unit root) — regressing a level, non-stationary dependent variable on a mix of level regressors (some also I(1)) is the textbook spurious-regression setup, the exact reason this whole module (and `bayesian_deviation_model.py` before it) works in first differences everywhere else. The rolling-window fit confirms the diagnosis directly, not just the pre-registered theory: dxy and carry both FLIP SIGN across the 162 rolling windows (dxy ranges −14.5 to +16.5, carry ranges −26.9 to +22.6) while every window's own R² still reads abnormally high (0.59-0.98 throughout) — coefficients that swing this wildly and cross zero aren't describing a stable structural relationship, they're tracking whatever nonstationary trend happens to co-move with deviation in that stretch. **Conclusion: the level R² of 0.871 is the artifact, not the finding — the delta-space R²≈0.58-0.61 remains the trustworthy number for this channel set.** Exploratory only, not wired into the dashboard tab.
  **New channel: curve_steep (BR nominal 10Y-2Y yield-curve steepening), same day, direct user request** ("try another variable to capture the fiscal risk: the curve steepening, ie. the difference between 10Y - 02Y"): an alternate, market-based fiscal-risk proxy alongside (not replacing) the CDS-based `fiscal` channel — the hypothesis being that rising long-run fiscal risk (debt-sustainability/monetization concerns) moves long-dated rates more than short, policy-anchored ones, steepening the curve independently of whether 5y CDS itself moves. Data: `base_mercado.interest_rates` (an EXTERNAL fund-ops schema, not this project's own ETL — same source `_load_breakeven()` already reads for PREJS@120M/NTNBJS@120M), `curve='PREJS'` (nominal pre-fixed/DI curve) at `tenor='120M'` (10Y) minus `tenor='24M'` (2Y), confirmed via direct query: both tenors have full daily coverage 2006-01-02→2026-07-29 (5133 obs each), no gaps. `ppp_equilibrium._load_curve_steepening(curves)` (new) applies the same `_PREJS_120M_BUG_WINDOWS` masking/interpolation `_load_breakeven()` already uses for its own PREJS@120M read (the confirmed bug windows are isolated to that one curve/tenor — PREJS@24M needs no equivalent treatment) and resamples to month-end. Refactored `_load_interest_rate_curves()` out of `_load_breakeven()` so the `base_mercado.interest_rates` table is read ONCE and shared between `_load_breakeven()` and `_load_curve_steepening()` — same precedent as `_load_policy_rates_monthly()` being shared by `_load_relative_carry()`/`_load_carry_vol_metrics()` — `_load_breakeven()`'s signature changed to take `curves` as a parameter accordingly. `bayesian_deviation_model.build_deltas_contemporaneous()` gained `curve_steep` in its delta-computation tuple so `delta_curve_steep` exists wherever `delta_fiscal` etc. already do, no other plumbing needed. **Not added to `ridge_deviation_model._CHANNELS`** (the shipped dashboard's channel set) and **not added to any raw-data tab-1 panel** — direct user instruction ("do not include in the dashboard yet, let's test") — so this is a new data column that exists in `load_data()`'s output but isn't yet surfaced anywhere in `ppp_dashboard.html`.
  **Shrunk AR(1) spec tested against it, same day, direct user request** ("let's shrink the model: a + AR(t-1) + Fiscal(t) + carry/vol(t) + DXY(t) + CurveSteep(t)"): `_CHANNELS_SHRUNK = ["fiscal", "carry_vol", "dxy", "curve_steep"]` (new module constant) plus `run_ar1_variant()` (new, generalizes the ad-hoc AR(1) test run earlier the same day into a reusable function, `channels`-generic so it covers both the full 8-channel spec and this shrunk one via a `label` parameter for its own `ridge_results/` CSVs). **Result, scored on a fair OOS comparison (both specs' samples aligned to their common date range, n=220, since curve_steep's own 2006-01 start would otherwise give the shrunk spec 2 extra months): shrunk (5 params: 4 channels + AR(1)) matches the shipped 9-parameter spec's whole-sample R² almost exactly (0.5827 vs. 0.5829) while landing at the walk-forward lambda FLOOR (0.01, vs. 3.16 for the full spec — barely any collinearity left to correct for with only 4 channels) and predicting BETTER out-of-sample (OOS MSE 8.64 vs. 8.90).** Whole-sample fit: alpha=+0.212, fiscal=+2.43 (essentially unchanged from the full spec), carry_vol=−0.96 (unchanged), dxy=+1.02 (unchanged), **curve_steep=+0.39** (positive — a steepening curve moves `delta_dev` the same direction as fiscal risk widening does, i.e. BRL deviates further from PPP, matching the hypothesis), phi(`delta_dev_lag1`)=−0.083 (consistent with every other AR(1) fit today). **Read: a genuinely more parsimonious model that isn't just "as good," it's slightly better OOS — dropping carry/relative_carry/relative_carry_vol/breakeven_gap/tot and adding curve_steep loses essentially nothing and gains a touch of out-of-sample accuracy.**
  **Moved into the tab, same day, direct user instruction ("Move it to tab")**: `build_dashboard_payload()`'s default `channels` switched from `_CHANNELS` (8 channels) to `_CHANNELS_SHRUNK` (fiscal, carry_vol, dxy, curve_steep) — the tab's actual model is now the 5-parameter shrunk AR(1) spec, not the 9-parameter full one. `_CHANNELS` itself is untouched (still the default for `run()`/`compare_lag_depths()`/`fit_lag_structure()`, all independent exploratory entry points), and passing `channels=_CHANNELS` explicitly to `build_dashboard_payload()` still reproduces the old 9-parameter tab if ever needed. `CHANNEL_ORDER_RIDGE`/`CHANNEL_LABELS_RIDGE`/`CHANNEL_COLORS_RIDGE`/`PARAM_LABELS_RIDGE` all shrunk to exactly the 5 entries now in the payload (`delta_fiscal`, `delta_carry_vol`, `delta_dxy`, `delta_curve_steep`, `delta_dev_lag1`) — `delta_curve_steep` is new to these maps, color `#4A90D9` (a medium blue, not used anywhere else in this file). The tab's intro gained a 2nd callout explaining `curve_steep` (definition, fiscal-risk-proxy rationale, same-sign cross-check against the CDS-based `fiscal` channel) and the 1st callout's channel-growth history was rewritten to end at "shrunk back down to 4" rather than "grown to eight," so the tab's own copy matches what it's actually showing rather than reading as a stale growth log. **Refreshed real-data result (n=222, 2008-01→2026-06 — 2 months longer than the old 9-parameter tab's 2008-01→2026-04, since curve_steep's own coverage constraint is looser than breakeven_gap/tot's): lambda=0.01 (the walk-forward floor — barely any collinearity left to correct for with only 4 channels), alpha=+0.212, fiscal=+2.43, carry_vol=−0.96, dxy=+1.02, curve_steep=+0.39, phi=−0.084, R²=0.583.** Verified the same two ways as every prior change to this tab: `node --check` on both files, template/shipped-file structural diff (zero non-marker-line differences), and the extracted IIFE executed against the real, currently-embedded payload in a stubbed DOM — content shown, exactly 6 param-select options (alpha + 4 channels + AR(1) term), all 4 charts fire, switching to `delta_curve_steep` and to `delta_dev_lag1` both don't throw. Decomposition/level-bridge reconstruction re-verified to ~3e-4. **Actual browser rendering not yet visually confirmed** — same standing caveat as the rest of this file.
  **Plain-regression variant, same day, direct user request** ("instead of considering the ppp as equilibrium, incorporate it in the regression as a channel... rerun without considering the deviation stuff, just the regression"): every other spec in this module regresses `delta_dev = 100*diff(log(ptax/equilibrium))`, which forces PPP's own implied move to have a coefficient of exactly 1 (full pass-through) by construction — subtracting it out in log space before anything is estimated. `build_plain_regression_sample()`/`run_plain_regression_variant()` (new) instead regress the exchange rate's own log return directly, `delta_fx(t) = 100*diff(log(ptax(t)))`, with PPP entering as ONE MORE CHANNEL — `delta_ppp(t) = 100*diff(log(ipca_index) - log(cpi_index))`, the BR-US relative inflation differential for that month, with its coefficient freely estimated by Ridge instead of assumed. No `compute_deviation()`/`compute_equilibrium()` call anywhere in this function — genuinely dependent-variable-free of the equilibrium framework, not just re-expressing it. No base month needed either, since `diff()` cancels whatever constant a base month would introduce (mirrors the base-month-invariance argument from earlier this session, applied in the other direction). Channels default to `_CHANNELS_SHRUNK` (fiscal, carry_vol, dxy, curve_steep) — same four already shown to carry signal; the dependent variable is what's being tested here, not the channel set. **Result (n=222, same sample as the shrunk deviation-framework spec): lambda=0.01 (floor), R²=0.5915 — slightly HIGHER than the deviation-framework shrunk spec's 0.583 on the same channels/sample.** Whole-sample fit: alpha=+0.505, **beta_ppp=+0.34** (well below the 1.0 the deviation framework assumes — the classic short-run PPP-puzzle finding, now showing up directly as a freely-estimated coefficient rather than an assumption baked into the dependent variable), fiscal=+2.43, carry_vol=−0.95, dxy=+1.03, curve_steep=+0.39, phi(`delta_fx_lag1`)=−0.077 — all four essentially unchanged from the deviation-framework model, a good cross-check that their signal doesn't depend on how PPP is handled. **Read with the same "single afternoon's read" caution as elsewhere in this file — specifically for beta_ppp**: the rolling-window mean is only **+0.04** (not +0.34), swinging from −0.86 to +1.48 across the 163 windows — the whole-sample point estimate looks driven by a specific stretch of the sample, not a stable relationship, unlike the other four channels' rolling paths which stay reasonably centered near their own whole-sample values.
  **PPP dropped entirely, same day, direct user follow-up** ("Remove the ppp entirely, let the alfa capture it"): `build_plain_regression_sample()`/`run_plain_regression_variant()` gained `include_ppp: bool = True` — `include_ppp=False` skips `delta_ppp` from the regressors altogether, letting alpha absorb whatever average drift PPP would otherwise have captured (mostly just Brazil's average inflation running above the US's over an 18-year sample, a near-constant monthly pull), same convention every other spec in this file already uses. **Result (n=222, same sample): lambda=0.01 (floor), R²=0.5873 — barely lower than with `delta_ppp` included (0.5915) — but OOS MSE at that lambda is 8.578, BETTER than the 8.712 with `delta_ppp` in.** Whole-sample fit: alpha=+0.481 (vs. +0.505 with PPP — barely moved), fiscal=+2.44, carry_vol=−0.94, dxy=+1.06, curve_steep=+0.39, phi(`delta_fx_lag1`)=−0.063 — all four essentially unchanged yet again. **Read: confirms the previous entry's rolling-instability finding directly rather than just flagging it** — `delta_ppp` was costing almost nothing to drop in-sample and genuinely improving out-of-sample prediction, the same overfitting signature the lag-structure comparison found earlier in this file (a channel whose whole-sample coefficient doesn't survive the rolling read isn't just "uncertain," it's actively adding noise a leaner spec predicts better without).
  **Wired into the dashboard, same day, direct user instruction ("Wired it into the dashboard")**: `build_dashboard_payload()` switched again, this time from `build_ar1_sample()`'s deviation-framework spec to `build_plain_regression_sample(channels=_CHANNELS_SHRUNK, include_ppp=False)` — the tab's dependent variable is now `delta_fx` (the exchange rate's own log return), not `delta_dev`, and there's no `compute_deviation()`/`compute_equilibrium()` call anywhere in the payload's fit path. **Level bridge simplified accordingly**: with no PPP/equilibrium concept left to bridge from, the base layer of `level_decomposition` changed from a time-varying `"equilibrium"` curve to a FLAT `"anchor"` series (the actual PTAX rate one month before the sample starts) — `anchor * exp(cum_alpha/100) * prod(exp(cum_contrib[c]/100)) * exp(cum_residual/100)` still reconstructs the actual PTAX rate exactly (re-verified to ~2e-4), just one layer shorter than every other tab's bridge. `delta_dev_lag1` renamed to `delta_fx_lag1` throughout (module code, both HTML files' `CHANNEL_ORDER_RIDGE`/`CHANNEL_LABELS_RIDGE`/`CHANNEL_COLORS_RIDGE`/`PARAM_LABELS_RIDGE`, and the JS's `key === 'equilibrium'` checks in the Ridge tab specifically became `key === 'anchor'` — the four OTHER tabs' own identical-looking `key === 'equilibrium'` checks, which have nothing to do with this change, were left untouched). Tab intro gained a callout explaining the dependent-variable switch and the PPP-in-then-out decision in one place, and the 1st callout's opening sentence (which described the now-gone deviation/channel-in-z-score setup) was trimmed since it no longer matches what the tab does. **Refreshed real-data result (n=222, 2008-01→2026-06, same sample as the deviation-framework version): lambda=0.01 (floor), alpha=+0.481, fiscal=+2.44, carry_vol=−0.94, dxy=+1.06, curve_steep=+0.39, phi(delta_fx_lag1)=−0.063, R²=0.5873** — slightly higher than the deviation-framework shrunk spec's 0.583 on the same channels/sample, consistent with the plain-regression variant's own earlier finding. Verified the same way as every prior change to this tab: `node --check` on both files, structural diff (zero non-marker differences), and the extracted IIFE executed against the real, currently-embedded payload — content shown, 6 param-select options, all 4 charts fire, switching to `delta_curve_steep` and `delta_fx_lag1` both don't throw. **Actual browser rendering not yet visually confirmed** — same standing caveat as the rest of this file.
  **Decomposition chart improved, same day, direct user request, two parts**: (i) "consider as one metric the anchor, the alfa and the AR(1), give it a name" — the anchor level, α, and the AR(1) term are the three pieces of this model that aren't one of the four actual economic channels, so `build_dashboard_payload()` now bundles them into a single **"Baseline (anchor + α + AR(1))"** bucket instead of three separate bars: `baseline_monthly = alpha + contributions["delta_fx_lag1"]`, cumsum'd and applied to the flat anchor level exactly the way the old "anchor"/"equilibrium" bucket worked (`anchor * exp(cum_baseline/100)`), so it's still an ABSOLUTE level in the chart, not a signed increment. `channel_cols` (delta_cols minus `delta_fx_lag1`) is a new local split used only for the decomposition/level-bridge loop — the ROLLING COEFFICIENT dropdown elsewhere on the tab still uses the full `delta_cols`/`CHANNEL_ORDER_RIDGE` unchanged, since seeing phi's own rolling path is still a legitimate, separate use of that parameter.
  (ii) "I want the option to select an initial date to make a decomposition... understand how my model explain the performance since 2025" — a REBASE, not the "zoom" convention the BEER "Rolling Window (Core 4)" tab's own start-date selector uses (which slices already-cumulative-since-2008 arrays, so every bar in a later window still carries the full pre-window buildup). Implemented instead as a genuine rebase: `build_dashboard_payload()` now also returns `contrib_monthly` — the MONTHLY, non-cumulative log-% contribution of each bucket (baseline, each of the 4 channels, residual) — and the new `ridgeDecompStart` `<select>` (populated from `D.months`, defaulting to `2025-01`, same `<select class="base-select">` pattern as `rollingDecompStart`) triggers a client-side `cumFromRg()` that re-sums each bucket's monthly contributions FROM the chosen start index (zeroed there, not carried from 2008), anchored at the actual PTAX rate the month before the chosen start (`L.actual[i0-1]`, or the original `anchor_level` if the earliest month is chosen). This is real, added complexity over a plain slice specifically so the bars for a "since 2025" view genuinely decompose the 2025-to-now move, not the 2008-to-now one just windowed for display. Verified two ways: the same Python-side reconstruction check as every other bridge (rebased from 2025-01: max error ~1.3e-5, tighter than the whole-sample view's ~2e-4 since less rounding accumulates over a shorter window) and a new harness assertion that re-triggers the `change` event, captures the actual Plotly traces passed to `ridgeDecompChart`, and checks the bars sum to the "Actual (PTAX)" line at every point in the rebased window (max error confirmed ~1.3e-5 in the harness too, matching Python exactly) — plus a second check that switching to a later start month (6 months from the end) produces a 6-point window. Both dashboard files' JS mirrored identically; structural diff still clean.
  **Carry-in-level variant, same day, direct user request ("Run with the carry in level")**: a second, parsimonious answer to the same "does a channel's effect persist beyond the month it happens" question the lag structure above addresses with 48 regressors — here, only `carry` is changed, from `delta_carry` (z-scored month-over-month change) to `carry_level` (z-scored raw level of the BR-US nominal policy-rate differential, `diferenciais_juros.diferencial_nominal`); every other channel keeps its own contemporaneous delta, unchanged. Rationale: with `delta_carry`, a persistently high but no-longer-rising carry stops contributing to `delta_dev` the month it stops changing; with `carry_level`, a persistently high carry keeps pulling `delta_dev` every month for as long as the level stays elevated — a level regressor against a differenced dependent variable is the standard error-correction-style way to get persistence without adding lag terms. `build_sample_carry_level()` (new) mirrors `build_sample()` but swaps in `df["carry"]` (z-scored against the same 2000-01+ reference window) for `delta_carry` in the regressor list; `run_carry_level_variant()` reuses `walk_forward_lambda()`/`fit_whole_sample()`/`rolling_fit()` completely unchanged (none of the three assume anything about a column's name). **Exploratory only, like `bayesian_deviation_model.py`'s own `primary_gap`/`primary_studentt`/`robustness` specs — not wired into the dashboard tab**, results saved to `ridge_results/carry_level_*.csv`.
  **Result (n=220, same sample as the 8-channel contemporaneous spec): lambda=5.109 (vs. 3.162 for the all-delta spec — slightly higher, consistent with a level regressor being smoother/more autocorrelated than a delta one, so the penalty leans a bit harder on it). R²=0.581, essentially flat vs. 0.578 for all-delta and well short of the lag structure's 0.605** — swapping one channel's representation buys almost nothing in-sample, unlike letting all 8 channels spread across 6 lags. `beta_carry_level=-0.563` (HDI-free point estimate, Ridge has none) — negative, same "wrong sign vs. UIP" direction the delta-form carry channel and every other model in this file that's tested carry keep finding, now expressed as a persistent pull rather than a one-month shock. The other seven betas are essentially unchanged from the all-delta 8-channel run (fiscal +2.367, dxy +1.089, relative_carry −0.113, carry_vol −1.073, relative_carry_vol +0.233, breakeven_gap +0.142, tot −0.094). Rolling read (161 windows, 60-month): R² mean 0.639 (range 0.428-0.761, a materially better average fit than the all-delta rolling read, though not a controlled comparison since the two specs aren't nested); `beta_carry_level` stays negative in every window (mean −0.525, range −1.411 to +0.076) — more consistently signed across time than `delta_carry`'s own rolling path was. **Read as a lead, not a conclusion**: R² comparisons across specs with different regressor representations (level vs. delta) aren't a clean apples-to-apples model-selection criterion on their own — if this direction is worth pursuing further, the natural next step is testing carry_level against an out-of-sample walk-forward comparison directly against the all-delta spec, not just the in-sample R².

**Election-risk dummy — tried, then CANCELLED same day**: built as a 7th dashboard tab ("Election Risk (Rolling)"), a binary dummy flagging the calendar months before each Brazilian presidential election's first-round date, added as a 5th channel alongside the core-4 BEER spec. Full record of what was tried and why it was pulled: see "Pending / next steps" below.

**Chart zoom/pan added dashboard-wide, same day (user request: "tools to explore the graph, select and zoom in/out, adjust axis, like ... TradingView", "implement in all graphs")**: `chartjs-plugin-zoom` (2.2.0, plus `hammer.js` for touch/pinch) added via CDN and registered globally (`Chart.register(ChartDataLabels, ChartZoom)`), with pan/zoom/drag-box config set ONCE on `Chart.defaults.plugins.zoom` rather than per chart — applies to literally every chart in every tab (current and future) without touching any individual chart's own options. Interaction: scroll/pinch to zoom, plain drag to box-zoom into a range, shift+drag to pan, double-click (global delegated listener via `Chart.getChart(canvas)`) to reset. A one-line hint above the tab bar makes the interaction discoverable. **New standing rule, not just a one-off**: `.claude/rules/lis-dashboards.md` documents this exact setup so it's applied the same way to `analytics/inflation/report.html`/`analytics/monetary_policy/report.html` next time either is touched, rather than only living in this one file. JS syntax checked with `node --check`; actual browser rendering not yet visually confirmed.

**Gesture mapping corrected to match real TradingView, dashboard-wide (2026-07, user request: "I want that the graph interaction to be more closely possible to the trading view, due to the fact that I'm used to manage graph there")**: the interaction above (plain drag = box-zoom, shift+drag = pan) was a reasonable design choice at the time but is actually *inverted* from how TradingView itself behaves. Real TradingView: plain drag pans, scroll/pinch zooms (X only), there's no box-zoom-via-drag gesture at all, and the Y (price) axis continuously auto-fits to whatever's visible rather than being user-zoomable directly. Rebuilt to match exactly: `Chart.defaults.plugins.zoom` now sets `pan:{mode:'x'}` (no modifier key), `zoom:{mode:'x', drag:{enabled:false}}`, plus a new hand-rolled `_bindYAutofit(chart)` wired to `onZoomComplete`/`onPanComplete` (and re-run after `resetZoom()`) that recomputes each y-axis's `min`/`max` from whatever datapoints are currently visible on X — generic across single/dual-axis (`yAxisID`) and plain/stacked (`scales[axisId].stacked`) bar datasets, and respects legend-hidden datasets. Also extended dashboard-wide to the three **Plotly** reports for the first time (`analytics/exchange_rate/report.html` itself, `analytics/inflation/report.html`, `analytics/monetary_policy/report.html`) via an equivalent `dragmode:'pan'` + `yaxis.fixedrange:true` + a Plotly-flavored `_bindYAutofit(divId)` (listens on `plotly_relayout`, category- and date-axis aware) — those three previously had no zoom/pan treatment at all (Plotly's own defaults: box-zoom-via-drag, no scroll-zoom, no Y-autofit). Full implementation, rationale, and verification approach now live in `.claude/rules/lis-dashboards.md` rather than being restated per-file. One real bug caught by a runtime harness before shipping (not by reading the code): the Plotly `_bindYAutofit` read `el.layout || el._fullLayout` to get the axis type, which is backwards — Plotly only resolves the auto-detected `xaxis.type` (category vs. date) onto `_fullLayout`, never back onto the raw `layout` object — fixed to `el._fullLayout || el.layout` in all three report files plus this dashboard's template. The Chart.js side (this file + `ppp_dashboard_template.html`) only got a mock-object unit test of `_bindYAutofit`'s core logic, not a full runtime harness (would need jsdom/node-canvas) — actual browser rendering still not visually confirmed for either library.

**Chart.js zoom/pan actually fixed, 2026-07-28** (user report while reviewing the dashboard: "the graph is not working yet", pointing at `reports/inflation_latest.html`'s working Plotly charts as the reference) — the "not yet visually confirmed" caveat above turned out to be hiding two real, confirmed bugs, found only by installing Playwright (`playwright-core` + a headless Chromium, `npx playwright install chromium`) and actually firing wheel/drag events against the real generated file, not just reading the code: **(1)** `Chart.defaults.plugins.zoom` (the standing convention documented above and in `.claude/rules/lis-dashboards.md`) silently breaks x-axis wheel-zoom/pan — y still moves, x never does — regardless of whether each chart ALSO sets its own `options.plugins.zoom` on top of the default; the only combination that actually works is never touching `Chart.defaults.plugins.zoom` and setting the full config on every chart's own `options.plugins.zoom` instead (via a shared `zoomPluginOpts()` helper, not hand-copied). **(2)** the implicit category x-axis every chart here had (from a plain `data.labels` string array) doesn't reliably zoom/pan either way (matches upstream chartjs-plugin-zoom issues #360/#655/#90) — fixed by giving the x-axis `type:'linear'` with explicit `min:0, max:N-1` and re-expressing every dataset's data as `{x:index, y:value}` pairs (`toXY()`) instead of a bare array, with a `ticks.callback` (`xAxis()`) mapping the index back to its month label. Both fixes were needed together — either alone still left x frozen. An initial round of testing wrongly concluded bar charts (the decomposition/nominal-bridge tabs) had a separate, additional limitation preventing x-zoom even with fix (2) applied — that testing still had bug (1) present; once (1) was actually fixed, bar charts zoom/pan on x exactly like line charts, no separate limitation ever existed. All 21 time-series charts across all 6 tabs (the 3 horizontal coefficient/forest bar charts and the posterior-histogram grids are correctly left untouched — not time series) now verified with a real Playwright harness: wheel-zoom and click-drag-pan both move the visible x/y range on every one, zero console/page errors, screenshots visually confirmed. `.claude/rules/lis-dashboards.md` and the `lis-dashboard` skill's own `references/design-system.md`/`SKILL.md` (same `Chart.defaults.plugins.zoom` pattern, presumed to share the same bug though not directly retested) were updated with the corrected recipe.

**Same day, immediate follow-up ("apply this to the Y axis too")**: the X-only + Y-autofit model above lasted about as long as it took the user to try it — they wanted the Y (price) axis directly pannable/zoomable too, same as most charting tools' ordinary free-pan/free-zoom default, not locked to an auto-fit. Widened to full `mode:'xy'` on both `pan` and `zoom` (Chart.js) and dropped `fixedrange` from every y-axis (Plotly) — on the Chart.js side this made `_bindYAutofit` entirely redundant (native `mode:'xy'` handling already moves Y correctly on every drag/scroll) so it was deleted outright, not just left unused. On the Plotly side `_bindYAutofit` earns its keep for a narrower reason: these reports also have rangeselector preset buttons (3a/5a/10a/Tudo) that jump the X window directly with no accompanying Y gesture at all, so something still has to fit Y for that specific case. Narrowed its trigger accordingly — it now fires only when `xaxis.range` changed *without* any `yaxis`/`yaxis2` range also changing in that same `plotly_relayout` event, so a direct drag or an xy scroll-zoom (which always move both axes together in one event) is left alone rather than fighting the user's own gesture. Re-verified with the same real-data runtime harness, extended to check both cases explicitly (X-only event → autofit fires; X-and-Y-together event → autofit stays out of the way) across all three reports including the dual-axis and category-axis charts — all passed. Full current model + rationale in `.claude/rules/lis-dashboards.md`, which has now flip-flopped twice in one day and documents why, to head off re-reverting it a third time without reading the history first.

Open threads on this concept note (next session should resolve or explicitly re-scope, not silently drop):
- **Partly resolved 2026-07-23**: the user settled the measurement-equation equilibrium question in favor of PPP — specifically the BRL/USD price-level ratio (headline IPCA/CPI), reasoning (i) the goal is understanding the *nominal* rate, not the real one, and (ii) the nominal rate empirically oscillates around this ratio over time. `ppp_equilibrium.py`/`ppp_dashboard.html` implement this as **relative** PPP (ratio of price-level *changes* since a selectable base month, anchored to the actual PTAX rate at that month) rather than absolute PPP (which would need internationally comparable price *levels*, not just indices) — the base-month dependence this introduces is real and is surfaced directly in the dashboard, not hidden. Still open: whether/how to fold this into the Kalman-filter measurement equation itself (the dashboard is a standalone exploration, not yet wired into any estimation code), and the second inline question — whether MA/time-varying parameters should be added, to test the hypothesis that the "important" fundamental changes over time — the same concern the doc's own Scapegoat Theory caveat already names.
- **Sign-convention consistency — resolved 2026-07-24 for the real model**: the doc's `rer` followed BIS REER convention (rise = appreciation), while the simulator's deviation chart assumed the opposite (rise = more depreciated) — never reconciled. Moot for `state_space_model.py`: the user chose Y = nominal ptax, which has one unambiguous convention (rise = BRL weaker) and already matches how `ppp_equilibrium.compute_equilibrium()` is anchored. Still worth a note if `state_space_simulator.html` or the concept note itself are revisited, since they're illustrative/REER-flavored and weren't rewritten to match.
- **Permanent vs. temporary pull-variable classification** (which of carry/terms-of-trade/fiscal-risk belongs in the transition vs. deviation equation) — **resolved for the toy simulator 2026-07-24, after a same-day false start**: an initial attempt put carry in the transition equation using an I(1)/I(0) argument borrowed from `bayesian_deviation_model.py` in isolation; corrected once it was clear that model's actual architecture puts **all** of carry/terms-of-trade/fiscal on the deviation side, with PPP alone occupying the equilibrium role — see the `state_space_simulator.html` entry above. Still open for the real Kalman-filter model itself: this is the toy simulator matching the Bayesian model's already-fitted structure, not an independent re-derivation — no stationarity test has been run directly against the state-space model's own specification, and no Beveridge-Nelson trend/cycle decomposition has been run at all.
- **New concrete input, 2026-07-23**: the Bayesian model's error-correction follow-up (`primary_ecm`, see `bayesian_deviation_model.py` above) found weak evidence that the PPP deviation mean-reverts with a median half-life of **~44 months**, matching the Rogoff PPP-puzzle literature. That's a real, data-derived candidate for calibrating (or as a prior on) how fast the state-space model's equilibrium-pull mechanism should be — bring it into the design discussion rather than re-deriving from scratch.

## Pending / next steps

- **Election-risk dummy — built, tested, then CANCELLED 2026-07-24 at user request ("this model should run with difference models")**: a binary dummy (1 in the N calendar months before each Brazilian presidential election's first-round date, 0 otherwise) was added as a 5th channel to the core-4 BEER spec (`_CHANNELS_CORE4_ELECTION`) and given its own 7th dashboard tab, mirroring "Rolling Window (Core 4)"'s structure exactly (rolling coefficient dropdown, R² chart, date-filterable decomposition). Tried at both a 6-month and 3-month pre-election window; neither flipped the whole-sample coefficient positive as the user expected (theory: election risk should widen the deviation, i.e. push `deviation(t)=100·ln(ptax/equilibrium)` positive, since that means BRL weaker than PPP-implied) — 6-month gave beta=-1.428 (94% HDI [-2.763,+0.020]), 3-month gave beta=-1.364 (HDI [-2.753,+0.113]), both negative and straddling zero. The 60-month rolling breakdown was more nuanced: 95 of 163 windows (58%) actually came out positive, swinging from -6.50 to +2.58 window to window — so the negative whole-sample average was being pulled down by a minority of large-negative windows, not a uniformly negative relationship. Only 4 complete election cycles (2010/2014/2018/2022) fall in-sample, so each rolling window's estimate rests on very few actual election episodes.
  **Cancelled, not just parked** — user's own diagnosis: this dummy was tested inside `beer_model.py`'s static, no-AR-term, LEVELS-only regression (deviation(t) explained by channels' contemporaneous levels, no `deviation(t-1)` term at all — see that module's docstring). The user's stated hypothesis is that election risk is better tested as a **"difference" (delta) model** instead — i.e. `state_space_model.py`'s AR(1)-with-regressors/delta-transform architecture (attempt two), not `beer_model.py`'s levels/no-AR spec. Concrete next step, not yet attempted: add the election dummy (as a level, the standard way a dummy enters even a delta-RHS model) to `state_space_model.py`'s `_CHANNELS`/`build_regressors()`, refit, and see whether the AR(1) structure — which already absorbs persistence separately from each channel's contemporaneous effect — produces a cleaner, more theory-consistent sign than the levels-only spec did. All code for the levels-only version (`_ELECTION_DATES`, `_build_election_dummy()`, `_CHANNELS_CORE4_ELECTION`, the 7th dashboard tab and its `/*ELECTION_DATA*/` marker) was fully removed from `beer_model.py`, `state_space_model.py`, `ppp_equilibrium.py`, and `ppp_dashboard_template.html` — nothing dead left in the codebase; the election first-round dates (2010-10-03, 2014-10-05, 2018-10-07, 2022-10-02, 2026-10-04) are worth reusing verbatim when this is retried.
- **"Attempt two" now has real code and a real fit** (`models/state_space_model.py`, 2026-07-24) — see the "State-space equilibrium model" section above for the full result and the honest read on how it disagrees with attempt one. Concrete next step, not yet done: let η be freely estimated (currently fixed at 0) to make this a genuine two-state Kalman filter rather than a single regression on an already-observed deviation series.
- **BOP "Financiamento Externo" — 10 lines with no SGS code identified**: asset-side bank/non-bank split, and the public/private/direct/other split within LP external loans (both inflows and amortizations). Two next steps identified, neither executed: (1) accept the coarser breakdown `balance_payments_breakdown.xlsx` already provides instead of forcing an exact match, or (2) search the BCB SGS series finder for codes outside the 22701–23060 range already in use.
- **Interest differentials are ex-post only** — ex-ante (Focus Selic/IPCA 12m for Brazil; Fed funds futures/OIS and Michigan survey or breakevens for the US) is not implemented. Add as new `_ex_ante`-suffixed series in `diferenciais_juros`, not a replacement of the existing ones.
- **Cotação tab shows only BRL/USD** — explicit user ask for EM peer currencies (MXN, CLP, COP) side by side; no spot series for those pairs exists in the DB yet (FRED has candidates like `DEXMXUS`) — blocked on authorization for new data collection, not on a technical blocker.
- **Fluxo cambial — CEP/CBE sub-items** (candidate SGS codes 24372–24376) returned data in an initial search but descriptions are unconfirmed — cross-check against the BCB's weekly Nota Cambial before using them.
- **Cupom cambial + B3 FX futures (DOL/WDO)** — deferred indefinitely; requires Bloomberg (`blpapi`/`xbbg`) on the running machine.
